# GATT Golden Vectors

This directory contains deterministic byte-level GATT codec vectors for KMP common migration. Transport ownership, notification subscription, callback ordering, readiness, permissions, reconnect logic, and public error mapping remain platform-owned unless a vector explicitly names a pure shared codec and the adapters keep their platform fallback path.
