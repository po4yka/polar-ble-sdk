# KMP Full-Coverage TDD Backlog

This backlog turns the pre-migration coverage inventory into executable TDD work. Use it after reading `KmpCoverageInventory.md`, `KmpTddStrategy.md`, `KmpFakeTransportTestPlan.md`, and `KmpValidationCommands.md`; the purpose is to choose the next failing-test slice without rerunning broad Gradle tasks after every small edit.

## Operating Rules

- Start each slice by adding or tightening characterization tests and golden vectors before moving production code.
- Prefer shared `commonTest` policy tests for pure byte parsing, model mapping, deterministic request planning, and fake transport runtime state.
- Keep Android-only and iOS-only BLE host behavior out of common code unless a slice first defines a platform-neutral contract.
- Do not migrate a behavior family while its row in `KmpCoverageInventory.md` still says `Partial` without a concrete deferral or a failing shared/common test.
- Treat every `platformExpectations.commonDecision`, top-level `commonDecision`, and `migrationOwnership` field as an explicit TDD decision point rather than background documentation.
- Batch several file/test/doc edits before Gradle validation so the migration does not stall on repeated manual app prompts.

## Current Executable Common Coverage

- Shared fixture loading: `GoldenVectorTestDataCommonTest.kt`.
- Shared fake transport contract: `FakeTransportContractCommonTest.kt`.
- Runtime planning and byte-codec policies: `RestRequestTransportPolicyCommonTest.kt`, `RestFacadeRuntimePolicyCommonTest.kt`, `RestServiceMappingCommonPolicyTest.kt`, `RestEventCompressionPolicyCommonTest.kt`, `FileFacadeRuntimePolicyCommonTest.kt`, `FileRuntimeErrorPolicyCommonTest.kt`, `OfflineTriggerRuntimePolicyCommonTest.kt`, `FirmwareUpdateUtilityCommonPolicyTest.kt`, `FirmwareWorkflowRuntimePolicyCommonTest.kt`, `BackupUtilityCommonPolicyTest.kt`, `PsFtpByteCodecCommonPolicyTest.kt`, `PsFtpRuntimePolicyCommonTest.kt`, `StreamRuntimePolicyCommonTest.kt`, `D2hStreamRuntimePolicyCommonTest.kt`, `CommandRuntimePolicyCommonTest.kt`, `StoredDataCleanupRuntimePolicyCommonTest.kt`, `DiskTimeRuntimePolicyCommonTest.kt`, `UserDeviceSettingsRuntimePolicyCommonTest.kt`, and `RemainingRuntimeWorkflowPlanningCommonTest.kt`.
- Protocol and model policy tests: `TypeUtilsCommonPolicyTest.kt`, `DeviceIdCommonPolicyTest.kt`, `TimeDateCommonPolicyTest.kt`, `DeviceCapabilitiesCommonPolicyTest.kt`, `AdvertisementCommonPolicyTest.kt`, `PmdSettingsCommonPolicyTest.kt`, `PmdControlPointCommonPolicyTest.kt`, `PmdSecretCommonPolicyTest.kt`, `EcgParserCommonPolicyTest.kt`, `AccParserCommonPolicyTest.kt`, `GyrParserCommonPolicyTest.kt`, `MagParserCommonPolicyTest.kt`, `PpgParserCommonPolicyTest.kt`, `PpiParserCommonPolicyTest.kt`, `PressureTemperatureParserCommonPolicyTest.kt`, `OfflineHrParserCommonPolicyTest.kt`, `OfflineRecordingMetadataCommonPolicyTest.kt`, `TrainingSessionCommonPolicyTest.kt`, `ExerciseSessionModelsCommonPolicyTest.kt`, `SkinTemperatureParserCommonPolicyTest.kt`, `SensorParserProductionCommonPolicyTest.kt`, `ActivitySummaryCommonPolicyTest.kt`, `SkinTemperatureDomainCommonPolicyTest.kt`, `DiskSpaceCommonPolicyTest.kt`, `Spo2CommonPolicyTest.kt`, `WatchFaceCommonPolicyTest.kt`, `KvtxCommonPolicyTest.kt`, `D2hNotificationCommonPolicyTest.kt`, `SleepNightlyRechargeCommonPolicyTest.kt`, `UserDeviceSettingsCommonPolicyTest.kt`, `SdLogModelsCommonPolicyTest.kt`, `FirstTimeUseModelsCommonPolicyTest.kt`, `StoredDataModelsCommonPolicyTest.kt`, and `GnssLocationOwnershipCommonPolicyTest.kt`.

## Remaining Shared-Policy Gaps

- Sensor parser gaps: PPG now has compile-verified common-owned raw/scalar/compressed, malformed raw/fixed/compressed payload, platform-split, and frame-family readiness policy coverage; GNSS/location remains explicitly Android-owned unless shared parity is deliberately added.
- SDK model gaps: disk-space and skin-temperature domain model mappings now have production shared KMP ownership on Android, including Android skin-temperature public enum lookup; training-session now has shared payload-read and parser-ownership policy vectors for fetch order, progress, malformed component omission, unknown advanced sample-list handling, and protobuf/gzip parser families, but real byte-level protobuf/gzip decoding still needs production common dependencies and compile verification before those payload parsers move to common code.
- Runtime/facade gaps: REST request and facade planning, file utilities, offline trigger, firmware, generic stream ordering, terminal-error propagation, checked and unchecked stream connection guards, generic consumer cancellation, duplicate stream completion, post-completion emission suppression, reset/sync/H10 command planning, stored-data cleanup workflow planning, disk/time query planning, and user-device-settings read/write planning have executable common planning/runtime policy coverage, Android offline-trigger facade mapping/error propagation is now covered through `BDBleApiImplTest.kt`, and PSFTP has executable raw RFC76/RFC60 byte-codec policy coverage plus Android/iOS Xcode/CocoaPods production delegation for header decoding, complete-message stream encoding, and zero-sequence RFC76 full-frame splitting, but production shared delegation still needs remaining facade compatibility tests, cancellation/error mapping tests, cleanup and sync failure compatibility decisions, and fake-clock or injectable-timeout coverage where applicable.
- Platform-owned gaps: Android Bluedroid host behavior, iOS CoreBluetooth host behavior, GATT client host interactions, and platform identifier routing should stay platform-specific unless a future slice defines a pure codec or deterministic state machine contract.

## Priority 0: Coverage Gates That Must Stay Green

- Keep `GoldenVectorMigrationPolicyTest` rejecting schema drift, missing consumer tests, missing shared common consumers for runtime planning vectors, accidental behavior in `shared/commonMain`, and missing docs for every common policy test.
- Keep `testdata/golden-vectors/README.md` and `testdata/golden-vectors/schema/golden-vector.schema.json` aligned with any new vector fields before adding vectors that use those fields.
- Keep `KmpValidationCommands.md` current whenever a new shared target, iOS command, Android module command, or local blocker changes.
- Keep `KmpCoverageInventory.md` updated in the same slice as new common policy tests so future migration work can find executable evidence quickly.

## Priority 1: Pure Protocol Slices

- Type utilities: Android production `TypeUtils` and iOS Xcode/CocoaPods `TypeUtils` now delegate selected unsigned byte/int/long and signed-int little-endian conversion through shared KMP after `TypeUtilsCommonPolicyTest` made `type-utils-readiness.json` executable; keep platform exception/null fallback policy, offset slicing, high-bit unsigned platform differences, SwiftPM/watchOS fallback, and downstream parser ownership explicit unless a later slice standardizes those public edge behaviors.
- Device ID and UUID utilities: Android and iOS Xcode/CocoaPods production consumption are now wired through shared KMP for platform-neutral checksum assembly, validation, and UUID conversion; keep platform-specific identifier routing outside common checksum/UUID conversion logic unless a later slice defines a separate shared identifier contract, and keep SwiftPM/watchOS fallback code until shared binary artifact packaging is defined.
- Time/date utilities: Android and iOS Xcode/CocoaPods production consumption are now wired through shared KMP for selected pure helpers including nanos/millis conversion, timezone offset minute/second conversion, duration-to-millis, time-string formatting, and plain-date validation; keep platform clock sourcing, timezone database behavior, Java `Calendar`/`LocalDateTime`, Swift `Date`, protobuf presence semantics, and SwiftPM/watchOS fallback code as adapter responsibilities until separate production consumption evidence exists.
- Product capability lookup: Android and iOS Xcode/CocoaPods production lookup now delegates the capability merge/lookup decision through shared KMP after `DeviceCapabilitiesCommonPolicyTest` made `capability-boolean-flags.json`, `capability-config-merge.json`, `capability-lookup-basic.json`, `capability-lookup-default-h10.json`, `capability-resource-override-ownership.json`, and `capability-lookup-readiness.json` executable from common tests; keep Android AssetManager/external-file selection and iOS Bundle.main/SDK-bundle/sandbox selection platform-owned unless a later slice deliberately introduces common resource loading with new platform tests.
- BLE advertisement parsing: Android and iOS Xcode/CocoaPods local-name model extraction and manufacturer HR payload extraction now delegate through shared KMP after `AdvertisementCommonPolicyTest` made `advertisement-readiness.json` executable; keep platform advertisement maps, RSSI filtering/window state, service UUID membership, public BLE advertisement objects, malformed/truncated fallback compatibility, and SwiftPM/watchOS fallback adapter-owned until shared binary packaging and broader advertisement parser policy are finalized.
- PMD settings: Android and iOS Xcode/CocoaPods compatible settings parsing, selected-setting TLV serialization, and setting-type code/name lookup now delegate through shared KMP after `PmdSettingsCommonPolicyTest` made `settings-readiness.json` executable; keep SwiftPM/watchOS fallback, Android-compatible parse-error behavior, iOS public unsigned value shape, unsupported SECURITY values, unknown setting types, and full public error mapping platform-owned until a later slice standardizes those edge policies.
- PMD control point and active measurement: Android and iOS Xcode/CocoaPods response parsing, PMD measurement-type ID lookup, PMD recording-type request-bit encoding, iOS active-measurement status-bit mapping, and PMD secret strategy serialization/from-byte lookup now delegate through shared KMP after `PmdControlPointCommonPolicyTest` made `control-point-readiness.json` executable; keep SwiftPM/watchOS fallback, short-payload trap compatibility, platform public enums, public error mapping, command request planning, offline-trigger runtime behavior, and platform transport execution adapter-owned until later runtime slices standardize those edges.
- PMD secret strategy: Android and iOS Xcode/CocoaPods PMD secret strategy serialization and known-byte lookup now delegate through shared KMP after `PmdSecretCommonPolicyTest` made `secret-readiness.json` executable; keep constructor error text, public error mapping, platform crypto/decryption, and unknown-strategy public behavior adapter-owned; full AES implementation ownership still must be chosen, and AES fixture outputs should be produced by production common code rather than by platform adapters or test-only shortcuts before removing platform fallbacks.

## Priority 2: Sensor Parser Slices

- ECG: Android delegates ECG parser production code through shared KMP and iOS Xcode/CocoaPods delegates valid raw type-0 parsing through `PolarIosSharedBridge` after `EcgParserCommonPolicyTest` made `ecg-readiness.json` executable; keep iOS unsupported raw type 1-3 behavior, malformed payload compatibility, Android-only type 1-3 ownership, SwiftPM/watchOS fallback, and typed deterministic malformed-frame policy decisions explicit before migrating more ECG behavior.
- ACC: Android delegates ACC parser production code through shared KMP and iOS Xcode/CocoaPods delegates valid raw type-0/type-1 plus compressed type-0/type-1 parsing through `PolarIosSharedBridge` after `AccParserCommonPolicyTest` made `acc-readiness.json` executable; keep iOS unsupported raw type-2 behavior, malformed raw/compressed payload compatibility, truncated compressed-frame policy, SwiftPM/watchOS fallback, and Android-owned raw type-2 parity decisions explicit before migrating more ACC behavior.
- GYR: Android delegates GYR parser production code through shared KMP and iOS Xcode/CocoaPods delegates valid compressed type-0 parsing through `PolarIosSharedBridge` after `GyrParserCommonPolicyTest` made `gyr-readiness.json` executable; keep unsupported raw/type behavior, Android-owned compressed type-1 parity, malformed/truncated payload compatibility, SwiftPM/watchOS fallback, and typed deterministic malformed-frame policy decisions explicit before migrating more GYR behavior.
- MAG: Android delegates MAG parser production code through shared KMP and iOS Xcode/CocoaPods delegates valid compressed type-0/type-1 and calibration-status mapping through `PolarIosSharedBridge` after `MagParserCommonPolicyTest` made `mag-readiness.json` executable; keep unsupported frame behavior, malformed/truncated payload compatibility, calibration public enum fallback, SwiftPM/watchOS fallback, and typed deterministic malformed-frame policy decisions explicit before migrating more MAG behavior.
- PPG: Android delegates PPG parser production code through shared KMP and iOS Xcode/CocoaPods delegates the covered raw/compressed PPG frame families through `PolarIosSharedBridge` after `PpgParserCommonPolicyTest` made the frame-family readiness manifests executable; keep platform sample-shape decisions, type-10 red/green split, malformed type-13 split, unsupported-type behavior, SwiftPM/watchOS fallback, and final malformed raw/fixed/compressed payload policy decisions explicit before removing platform fallbacks.
- PPI: Android delegates PPI parser production code through shared KMP and iOS Xcode/CocoaPods delegates valid raw type-0 parsing through `PolarIosSharedBridge` after `PpiParserCommonPolicyTest` made `ppi-readiness.json` executable; keep unsupported raw/compressed behavior, malformed raw-sample compatibility, SwiftPM/watchOS fallback, zero-timestamp/backfill policy visibility, and typed deterministic malformed-frame policy decisions explicit before removing platform fallbacks.
- Pressure and temperature: Android delegates pressure and temperature parser production code through shared KMP and iOS Xcode/CocoaPods delegates valid raw type-0 pressure/temperature parsing through `PolarIosSharedBridge` after `PressureTemperatureParserCommonPolicyTest` made `pressure-temperature-readiness.json` executable; keep compressed pressure one-channel indexing, compressed temperature sample-count, malformed/truncated payload compatibility, unsupported-frame behavior, SwiftPM/watchOS fallback, and compressed parity decisions explicit before migrating more behavior.
- Skin temperature: Android delegates skin-temperature parser production code through shared KMP and iOS Xcode/CocoaPods delegates valid explicit-rate raw type-0 parsing through `PolarIosSharedBridge` after `SkinTemperatureParserCommonPolicyTest` made `skin-temperature-readiness.json` executable; keep default-rate timestamp estimation, iOS empty malformed payload deferral, unsupported/compressed behavior, malformed/truncated payload compatibility, SwiftPM/watchOS fallback, and typed deterministic malformed-frame policy decisions explicit before removing platform fallbacks.
- Offline HR: Android delegates offline-HR parser production code through shared KMP and iOS Xcode/CocoaPods delegates valid raw type-0/type-1 parsing through `PolarIosSharedBridge` after `OfflineHrParserCommonPolicyTest` made `offline-hr-readiness.json` executable; keep unsupported compressed/raw behavior, malformed tuple compatibility, SwiftPM/watchOS fallback, and typed deterministic malformed-frame policy decisions explicit before removing platform fallbacks.
- GNSS/location: keep Android-owned for the first migration after `GnssLocationOwnershipCommonPolicyTest` is compile-verified for GNSS vectors plus `gnss-location-readiness.json`, the `gnss-location-readiness` manifest id, `androidOwnedPreMigrationCharacterization`, `android-owned-raw-type0-coordinate`, `android-owned-raw-type1-satellite-dilution`, `android-owned-raw-type2-satellite-summary`, `android-owned-raw-type3-nmea`, `non-ios-parser-ownership`, `non-common-parser-ownership`, `future-shared-parser-parity-gate`, platform GNSS vector references, and the `compile-verification-gate` unless a new Swift/common parser is deliberately added and all GNSS vectors become shared executable tests.

## Priority 3: SDK Model Mapping Slices

- Offline recording metadata: Android and iOS Xcode/CocoaPods filename mapping plus PMDFILES.TXT V2 grouping now delegate through shared KMP after `OfflineRecordingMetadataCommonPolicyTest` made `metadata-readiness.json` executable, and iOS Xcode/CocoaPods PMDFILES.TXT, recording-file, and sub-recording directory GET header selection now consumes shared file facade planning; keep trigger status parsing, missing-file policy, representative-path platform differences, recursive traversal, parser dispatch, byte-level PMD control-point adapter ownership, PSFTP/file transport execution, public enum projection, and facade error mapping adapter-owned until later runtime slices standardize those edges.
- Exercise sessions: Android and iOS Xcode/CocoaPods sport-profile id mapping now delegates through shared KMP after `ExerciseSessionModelsCommonPolicyTest` made exercise-session lookup executable, and offline Exercise V2 plus H10 exercise sample fetch/removal and `/DEVICE.BPB` capability-read GET/REMOVE header selection now consumes shared file facade planning on Android and linked iOS; keep exercise start/stop command construction, sample/status/capability protobuf parsing, filesystem-type gates, BLE transport execution, request/response errors, public facade behavior, and SwiftPM/watchOS fallback adapter-owned until a later runtime slice pins those surfaces through public facade tests.
- Training sessions: Android and iOS Xcode/CocoaPods reference aggregation, exercise classification, and exercise file-size planning now delegate through shared KMP after `TrainingSessionCommonPolicyTest` made `training-session-readiness.json` and `payload-parser-policy.json` executable, and directory-list, summary, exercise, and delete PFTP GET/REMOVE header selection now consumes shared file facade planning on Android and linked iOS; keep recursive traversal/date filtering, platform exercise path shape, missing-file behavior, protobuf/gzip payload reads, byte-level parser dependency gates, malformed component policy, advanced sample-list parsing, public model projection, delete target policy, and SwiftPM/watchOS fallback adapter-owned; add real common protobuf/gzip production dependencies and compile-verified byte decoding before moving those payload parsers.
- Activity samples and daily summaries: Android and iOS Xcode/CocoaPods automatic HR trigger, PPi trigger/status, activity-class, daily-balance, and training-readiness mapping now delegate through shared KMP after `ActivitySummaryCommonPolicyTest` made `activity-summary-readiness.json` executable, and activity directory/file, automatic-sample directory/file, and daily-summary PFTP GET header selection now consumes shared file facade planning on Android and linked iOS; keep protobuf/time parsing, PPi delta decompression, activity traversal, daily-summary scalar/duration projection, malformed activity-sample policy, unsupported-field deferral, public model shape, and SwiftPM/watchOS fallback adapter-owned until a full shared DTO migration deliberately changes those surfaces.
- Sleep and nightly recharge: Android and iOS Xcode/CocoaPods sleep wake-state/rating enum mapping now delegates through shared KMP after `SleepNightlyRechargeCommonPolicyTest` made `sleep-nightly-readiness.json` executable, and sleep plus nightly PFTP GET header selection now consumes shared file facade planning on Android and linked iOS; keep protobuf parsing, date/time conversion, nightly null/default policies, sleep end-offset platform difference, partial-night optional policy, public model projection, and SwiftPM/watchOS fallback adapter-owned until a full shared sleep/nightly model migration chooses compatibility policy.
- Skin temperature domain utilities: Android public enum lookup and iOS Xcode/CocoaPods measurement/sensor-location mapping now delegate through shared KMP after `SkinTemperatureDomainCommonPolicyTest` made `skin-temperature-domain-readiness.json` executable, and skin-temperature PFTP GET header selection now consumes shared file facade planning on Android and linked iOS; keep the `sourceDeviceId` common public model decision, SwiftPM/watchOS fallback, protobuf decoding, sample projection, and unknown-enum fallback policies adapter-owned until a later model-shape migration deliberately changes them.
- User device settings: Android and iOS Xcode/CocoaPods enum/value lookup plus selected serialization mapping now delegate through shared KMP after `UserDeviceSettingsCommonPolicyTest` made `sdk/user-device-settings/settings-model-readiness.json` executable; facade read/write orchestration may move only after `UserDeviceSettingsRuntimePolicyCommonTest.kt` remains compile-verified for `sdk/user-device-settings-runtime/settings-runtime-policy.json`, Android and iOS facade tests continue to reference the same vector, and protobuf serialization, platform defaults, daylight-saving time-source behavior, transport execution, and public error mapping remain explicit facade responsibilities.
- SD log configuration: Android and iOS Xcode/CocoaPods trigger/frequency enum projection now delegates through shared KMP after `SdLogModelsCommonPolicyTest` made SD-log model lookup executable, and SD-log config GET/PUT header selection now consumes shared file facade planning on Android and linked iOS; keep protobuf message construction, optional field presence, session notifications, BLE transport/write execution, and public error behavior adapter-owned until a later runtime slice pins those surfaces through public facade tests.
- First-time-use physical configuration: Android and iOS Xcode/CocoaPods gender, training-background, and typical-day enum selection/projection now delegate through shared KMP after `FirstTimeUseModelsCommonPolicyTest` made FTU model lookup executable, and user-id plus physical-config GET/PUT header selection now consumes shared file facade planning on Android and linked iOS; keep date parsing/building, protobuf message construction, validation ranges, sync sequencing, BLE transport/write execution, SwiftPM/watchOS fallback, and public error mapping adapter-owned until a later runtime slice deliberately migrates execution.
- Disk space: Android production and iOS Xcode/CocoaPods byte-total mapping now delegate through shared KMP after `DiskSpaceCommonPolicyTest` made `disk-space-readiness.json` executable; platform adapters still own protobuf parsing, malformed-payload errors, Android signed-fragment and iOS unsigned-fragment protobuf surfaces, SwiftPM/watchOS fallback, and disk/time facade query/error behavior.
- SPo2: Android enum factories and scalar public-model projection plus iOS Xcode/CocoaPods enum boundary mapping and scalar DTO projection now delegate through shared KMP after `Spo2CommonPolicyTest` made `spo2-readiness.json` executable and `PolarSpo2Models.projectTestData` promoted optional scalar projection, common empty recording-device normalization, nullable trigger-type policy, and unknown-enum boundaries to production common code, and SPO2TEST directory/file PFTP GET header selection now consumes shared file facade planning on Android and linked iOS; Android still preserves its public empty-string recording-device behavior while using shared scalar projection for the remaining pure fields. Fuller SPo2 migration still needs explicit decisions for Android trigger-type exposure, date/folder timestamp policy, directory traversal, protobuf parsing, stream emission, and SwiftPM/watchOS fallback before those surfaces move out of platform adapters.
- Watch face: Android and iOS Xcode/CocoaPods complication ID lookup now delegates through shared KMP after `WatchFaceCommonPolicyTest` made `watch-face-readiness.json` executable, and watch-face KVTX GET/PUT header selection now consumes shared file facade planning on Android and linked iOS; keep FlatBuffer/KVTX byte encoding and decoding, BLE/file I/O, unknown raw complication preservation, invalid semantic configuration policy, byte-for-byte output parity, public enum shapes, and SwiftPM/watchOS fallback adapter-owned unless a later byte-codec migration deliberately moves those surfaces.
- KVTX: Android `KvtxScriptUtils` and iOS Xcode/CocoaPods `KvtxScriptUtils` now delegate write-and-commit framing, extraction, unsigned-key handling, and typed malformed-script parsing through shared KMP after `KvtxCommonPolicyTest` made `kvtx-readiness.json` executable; keep iOS nil-on-truncation compatibility, platform public error behavior, FlatBuffer wrapping ownership, BLE/file I/O, and byte-for-byte platform output parity decisions explicit before removing platform fallbacks.
- Watch-face FlatBuffer and KVTX byte-identical output remains an explicit production common codec ownership decision; do not treat semantic common policy coverage as proof that shared FlatBuffer builders, shared KVTX byte emission, or byte-for-byte platform output parity exists.
- D2H notifications and stream runtime: notification ID mapping, unknown-ID filtering, emission planning, and parsed-protobuf selector lookup now delegate through shared KMP on Android and linked iOS while platform adapters still own generated protobuf parsing, public enum fallback, stream transport, and public error mapping; migrate stream orchestration only after `D2hStreamRuntimePolicyCommonTest.kt` is compile-verified for `stream-runtime-policy.json` and `stream-runtime-readiness.json`, `StreamRuntimePolicyCommonTest.kt` is compile-verified for generic stream policy vectors including `ordered-emissions-policy.json`, mapped values before late errors, cancellation cleanup, disconnect-after-subscription cleanup and terminal mapping, post-cancel suppression, failed-subscribe observer behavior, generic consumer cancellation, duplicate completion, post-completion emission suppression, platform facade error mapping, and the `compile-verification-gate` are accepted.
- Compression helpers: Android and iOS Xcode/CocoaPods uncompressed REST event payload ordering now delegates through shared KMP after `RestEventCompressionPolicyCommonTest` made `rest-event-compression-platform-policy.json` and `rest-event-compression-readiness.json` executable for uncompressed batches, empty batches, notification order, Android gzip vs iOS deflate split, malformed compressed payload split, platform event vector references, the `compile-verification-gate`, and the normalize-or-preserve codec decision. Keep gzip/deflate byte decoding platform-owned until a later slice either deliberately preserves the platform codec split in common adapters or standardizes it with byte-level shared codec vectors and public malformed-payload compatibility tests; add byte-level shared codec vectors before moving gzip/deflate behavior into common code.

## Priority 4: Runtime and Facade Slices

- REST and file utilities: Android and iOS Xcode/CocoaPods REST service list/description projection now delegates through shared KMP after `RestServiceMappingCommonPolicyTest.kt` made `sdk/rest-service/rest-service-mapping-readiness.json` executable; REST facade request planning, low-level file facade planning, and file runtime-error classification are now shared production code in `PolarRuntimeOrchestration.kt`, Android plus linked iOS REST facade GET path selection and low-level file helpers consume shared command/path operation selection, Android recursive file-list GET headers and iOS directory-empty GET headers now consume shared command/path selection, and the behavior remains compile-verified through `RestFacadeRuntimePolicyCommonTest.kt`, `FileFacadeRuntimePolicyCommonTest.kt`, and `FileRuntimeErrorPolicyCommonTest.kt` against their runtime policy and readiness vectors. Migrate platform facade execution only after Android and iOS facade tests continue to reference the same vectors, service-list/service-description success and request-failure behavior families remain listed, model JSON mapping references plus public response-error and empty-response compatibility are pinned, directory missing status 103, malformed directory payload parse failure, read transport errors, write PUT header and payload capture before stream failure, delete response-error status/message mapping, command/path capture, request paths, response errors, empty responses, malformed directory/protobuf behavior, directory traversal references, write-stream failures, facade-error mapping, and the `compile-verification-gate` keep public semantics.
- Backup workflow: Android and iOS Xcode/CocoaPods default backup paths and restore PUT planning now delegate through shared KMP after `BackupUtilityCommonPolicyTest.kt` made `backup-expansion-and-restore-writes.json`, `restore-failure-platform-policy.json`, and `backup-workflow-readiness.json` executable for `BACKUP.TXT` expansion, directory expansion, backup file read order, restore PUT command planning, payload preservation, restore write order, restore failure platform split, restore failure aggregation decision, platform facade error mapping, and the `compile-verification-gate`; Android and linked iOS restore headers consume shared PUT command/path selection, and Android plus iOS backup root/file/directory read headers consume shared GET command/path selection. Keep `BACKUP.TXT` parsing, directory traversal, PFTP read/write execution, restore failure aggregation/split behavior, and public facade error mapping adapter-owned unless a later slice deliberately migrates execution with platform tests still referencing the same vectors.
- PSFTP byte codec and runtime: Android and iOS Xcode/CocoaPods RFC76 header decoding, complete-message stream encoding, zero-sequence RFC76 full-frame splitting, write-progress platform policy, and write-ack success/timeout planning now delegate through shared KMP after `PsFtpByteCodecCommonPolicyTest.kt` and `PsFtpRuntimePolicyCommonTest.kt` made `byte-codec-readiness.json`, runtime vectors, and `psftp-runtime-readiness.json` executable for `rfc76-header` decoding, Android/iOS error-code split pinning, complete-message-stream encoding, Android file-data append behavior, MTU splitting, sequence wrap, request-response reassembly, notification reassembly, write progress platform-split pinning, notification error policy split pinning, initial-silence no-emission, consumer-owned virtual-clock timeout cleanup, continuation timeout, write interruption, write transport failure, write-ack timeout, and platform vector references. Keep nonzero-sequence incremental streaming, public error mapping, notification transport behavior, BLE transport execution, actual timeout scheduling, response observation, and facade-compatible write-progress emission adapter-owned before broader shared runtime execution delegation, and keep the `compile-verification-gate` explicit.
- Offline trigger runtime: Android and iOS Xcode/CocoaPods set/get facade planning now delegates through shared KMP after `OfflineTriggerRuntimePolicyCommonTest.kt` made `trigger-runtime-policy.json` and `trigger-runtime-readiness.json` executable for typed set-mode, status-read, settings writes, secret attachment, get/set transport errors, enabled/excluded feature behavior, facade error mapping, response-queue cleanup, and behavior-family coverage; Android public facade mapping and get/set error propagation are pinned in `BDBleApiImplTest.kt`, iOS public facade mapping, secret propagation, set-mode error propagation, and get-status error propagation are pinned in `PolarBleApiImplTests.swift`, dedicated byte-level Android/iOS PMD adapter packet framing is pinned in `BlePmdClientTest.kt` plus `BlePmdClientTest.swift`, and platform cleanup is pinned through Android stale wrong-command response discard plus iOS pre-command response queue clearing. Keep PMD packet construction, BLE transport execution, response-queue mutation, public error mapping, and cancellation behavior adapter-owned unless a later slice introduces shared production trigger orchestration with cancellable tasks, observers, or streams.
- Firmware workflow: Android and iOS Xcode/CocoaPods firmware utility helpers, package write ordering, and write-workflow status/file-order planning now delegate through shared KMP after `FirmwareUpdateUtilityCommonPolicyTest.kt` and `FirmwareWorkflowRuntimePolicyCommonTest.kt` made `sdk/firmware-update/utility-readiness.json`, `workflow-runtime-policy.json`, and `workflow-runtime-readiness.json` executable for device-info protobuf mapping, zero-version preservation, dotted-integer version comparison, invalid-version typed parse failure, `SYSUPDAT.IMG` file ordering, fake network, fake filesystem/zip extraction, fake BLE writer progress, cleanup, cancellation, retryable fake-network server failure classification, retry scheduling, terminal failures, platform facade error mapping, and behavior-family coverage; Android and iOS firmware info reads consume shared `GET` command/path selection for `/DEVICE.BPB`, and Android firmware writes consume shared `PUT` command/path selection for the planned package write order. Keep network download, zip extraction, BLE writer execution, progress throttling, cleanup callbacks, cancellation mechanics, retry scheduling, terminal server/device failure mapping, and public facade error mapping adapter-owned unless a later slice deliberately migrates executable workflow orchestration with the same platform vectors pinned.
- Reset, sync, and H10 recording commands: command planning for H10 queries, reset flags, sync notification sequencing, and terminal classification is now shared production code in `PolarRuntimeOrchestration.kt`, Android plus linked iOS production H10 recording, reset, and sync facades consume shared query/notification selection, and the behavior remains compile-verified through `CommandRuntimePolicyCommonTest.kt` against `reset-sync-h10-command-policy.json` and `reset-sync-h10-command-readiness.json`; migrate platform facade execution only after Android and iOS facade tests continue to reference the same vectors, protobuf builders and BLE transport calls are adapter-pinned, H10 query/reset flag/sync sequencing behavior families and the `compile-verification-gate` remain listed in the readiness manifest, and the Android/iOS sync-start and sync-stop failure split is either intentionally preserved in adapters or reconciled with an explicit public API compatibility decision.
- Stored-data cleanup workflows: Android and iOS Xcode/CocoaPods traversal filtering, empty-parent pruning decisions, and platform-split empty-parent path planning now delegate through shared KMP after `StoredDataCleanupRuntimePolicyCommonTest.kt` made `cleanup-workflow-policy.json` and `cleanup-workflow-readiness.json` executable; keep directory listing, remove execution, cleanup list-failure behavior, transport execution, and public facade error mapping adapter-owned unless they are intentionally preserved in adapters or reconciled with an explicit public API compatibility decision.
- Disk and time facade runtime: query planning for disk-space, local-time, local-time-with-zone, V2 two-query time setting, H10 single-query time setting, daylight-saving local-time update, and transport-error terminals is now shared production code in `PolarRuntimeOrchestration.kt`, Android plus linked iOS production disk/time facades consume the shared query sequence for reads and time-setting writes, and the behavior remains compile-verified through `DiskTimeRuntimePolicyCommonTest.kt` against `disk-time-query-policy.json` and `disk-time-query-readiness.json`; migrate platform facade execution only after Android and iOS facade tests continue to reference the same vectors, protobuf builders and BLE transport calls are adapter-pinned, and filesystem capability gates plus public error mapping remain platform facade responsibilities.
- User-device-settings facade runtime: read/write planning is now shared production code in `PolarRuntimeOrchestration.kt`, Android and linked iOS production settings helpers consume shared read/write command/path header selection, the standalone iOS settings utility consumes shared read command/path selection, and the behavior remains compile-verified through `UserDeviceSettingsRuntimePolicyCommonTest.kt` against `sdk/user-device-settings-runtime/settings-runtime-policy.json` and `settings-runtime-readiness.json`; migrate platform facade execution only after Android and iOS facade tests continue to reference the same vectors, settings path, protobuf preservation, read-failure no-write behavior, read-then-write payload planning, write-failure-after-payload behavior, daylight-saving payload shape, facade-error mapping, and the `compile-verification-gate` remain pinned through platform public APIs.
- Public SDK facade: do not delegate any public operation to shared code until both platform facade tests pin success, request failure, response failure, write failure, cleanup, cancellation, and platform-specific compatibility behavior.
- BLE device lifecycle and GATT clients: keep platform-owned unless a slice extracts a pure codec or deterministic state machine with common fake-transport tests.

## Non-Gradle Checks To Run While Batching

- `git diff --check`
- `ruby scripts/kmp_non_gradle_checks.rb`
- Optional diagnostic: `rg -n "digitToInt|toBooleanStrict|uppercase\\(|lowercase\\(|replaceFirstChar|ifEmpty|UL|UInt|UByte|ULong|java\\.|android\\.|com\\.google" sources/Android/android-communications/shared/src/commonTest/kotlin/com/polar/sharedtest` should print only reviewed allowlisted lines.

## Gradle Batch Point

Run Gradle only after a meaningful batch of shared common tests, vector guards, and docs has landed. The next useful Gradle batch should verify the shared module common/JVM tests plus the Android migration-policy test, then any touched Android-hosted characterization tests; before treating a broad Android coverage-preparation batch as merge-ready, run `:library:testSdkDebugUnitTest` as the full SDK debug unit-test gate. Do not run broad app/example tasks unless the slice actually changes those surfaces.
