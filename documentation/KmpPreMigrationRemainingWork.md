# KMP Pre-Migration Remaining Work

This document records what is still required before the full pre-migration coverage goal can be considered complete. It is intentionally a closeout handoff for the current coverage-preparation goal state, not a new migration plan.

## Current Verified State

- Golden-vector metadata and documentation policy checks pass with `ruby scripts/kmp_non_gradle_checks.rb`; the current vector count is 367.
- Whitespace safety passes with `git diff --check`.
- iOS test sources parse with `swiftc -parse sources/iOS/ios-communications/Tests/**/*.swift`.
- Focused shared/Android fake-transport validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest :library:testSdkDebugUnitTest --tests 'com.polar.testutils.FakeTransportContractTest' --no-daemon`.
- The broad Android SDK unit-test gate passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --no-daemon`.
- The shared target compile gate passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:compileAndroidMain :shared:compileAndroidHostTest :shared:compileKotlinIosX64 :shared:compileKotlinMetadata :shared:jvmTest --no-daemon`.
- The shared artifact smoke gate passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:bundleAndroidMainAar :shared:linkDebugFrameworkIosX64 --no-daemon`.
- The iOS validation probe passes with `ruby scripts/ios_xcode_validation_probe.rb`; CocoaPods support files are installed, the workspace is discoverable by Xcode, and iOS simulator destinations are available.
- The iOS XCTest gate is unblocked and passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5'`; the refreshed local run used the equivalent ID-pinned destination `platform=iOS Simulator,id=0DB7D4B5-705D-4195-8F51-75425EBF2B96,arch=arm64` and wrote `/tmp/polar-ios-xctest-unblocked.xcresult`. Shared golden-vector lookup now uses bundled `testdata` resources in both XCTest bundles, while source-checkout discovery remains only as a fallback.
- Focused iOS PSFTP characterization now passes for notification reassembly, write-success progress, timeout-planning metadata, and direct RFC76 raw-stream frame splitting with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/BlePsFtpClientTest/testPsFtpNotificationGoldenVectorsReassembleCompleteNotifications -only-testing:iOSCommunicationsTests/BlePsFtpClientTest/testPsFtpResponseGoldenVectorsCharacterizeWriteSuccessProgress -only-testing:iOSCommunicationsTests/BlePsFtpClientTest/testPsFtpTimeoutPlanningVectorsRequireFakeClockBeforeSharedRuntimeMigration -only-testing:iOSCommunicationsTests/BlePsFtpUtilityTest/testMessageStreamGoldenVectorsSplitRfc76Frames`.
- Focused PMD settings characterization now passes on iOS with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/PmdSettingTest/testPmdSettingGoldenVectorsMatchIOSCommunicationsBehavior`; Android PMD settings plus `GoldenVectorMigrationPolicyTest` also pass with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.PmdSettingTest' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon`.
- Focused PPG/temperature sensor characterization now passes on iOS with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/PpgDataTest/testPpgGoldenVectorsMatchIOSCommunicationsBehavior -only-testing:iOSCommunicationsTests/TemperatureDataTest/testTemperatureGoldenVectorsMatchIOSCommunicationsBehavior`; Android PPG/temperature/pressure plus `GoldenVectorMigrationPolicyTest` pass with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.PpgDataTest' --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.TemperatureDataTest' --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.PressureDataTest' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`, and the shared parser policy slice passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PpgParserCommonPolicyTest' --tests 'com.polar.sharedtest.PressureTemperatureParserCommonPolicyTest' --no-daemon --warning-mode all`.
- Focused REST service mapping and user-device-settings model characterization now passes on iOS with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDeviceRestApiServiceTests/testRestServiceGoldenVectorsMapJsonToPublicModels -only-testing:PolarBleSdkTests/PolarUserDeviceSettingsUtilsTest/testUserDeviceSettingsGoldenVectorsMatchIOSBehavior`; Android REST/user-device-settings plus `GoldenVectorMigrationPolicyTest` pass with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarDeviceRestApiUtilsTest' --tests 'com.polar.sdk.api.model.PolarUserDeviceSettingsTest' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`, and the shared common policy slice passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.RestServiceMappingCommonPolicyTest' --tests 'com.polar.sharedtest.UserDeviceSettingsCommonPolicyTest' --no-daemon --warning-mode all`.
- Android, iOS, and shared common REST service mapping readiness guards now assert exact policy-vector path order, exact migration-readiness state, exact required/covered behavior-family order, and Android/iOS/common consumer ownership instead of set equality or consumer presence-only checks for service-list name/path mapping, empty defaults, service-description action/event mapping, wrong-type service-list platform split, unknown-field policy, platform vector references, and compile verification; focused Android/shared validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.RestServiceMappingCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarDeviceRestApiUtilsTest.restServiceMappingReadinessManifest_isPinnedBeforeModelMigration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDeviceRestApiServiceTests/testRestServiceMappingReadinessManifestIsPinnedBeforeModelMigration -resultBundlePath /tmp/polar-ios-rest-service-mapping-readiness-order-exact.xcresult`.
- REST service mapping readiness now records `compileVerifiedPreMigrationCharacterization` instead of `blockedUntilCompileVerified`; the refreshed focused Gradle validation passed with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.RestServiceMappingCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarDeviceRestApiUtilsTest.restServiceMappingReadinessManifest_isPinnedBeforeModelMigration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`, and the corrected iOS XCTest selector `xcodebuild test -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme PolarBleSdk -destination 'platform=iOS Simulator,id=0DB7D4B5-705D-4195-8F51-75425EBF2B96,arch=arm64' -only-testing:PolarBleSdkTests/PolarDeviceRestApiServiceTests/testRestServiceMappingReadinessManifestIsPinnedBeforeModelMigration` executed one test instead of the stale zero-test `PolarDeviceRestApiTests` selector.
- Shared REST service wrong-type policy coverage now asserts exact wrong-type policy vector id/case/kind, Android empty service-list shape including empty pathsForServices, iOS decode-error flag, exact common decision text, exact notes, exact Android/iOS/common consumer ownership, and exact platform flags instead of substring matching in `RestServiceMappingCommonPolicyTest.kt`.
- REST service common production code now owns decoded service-list and service-description projection through `PolarRestServiceModels.serviceList` and `serviceDescription`, including empty defaults and action/event detail/trigger mapping, while platform adapters still own JSON decoding and wrong-type payload error policy.
- Android and iOS REST public model accessors now delegate decoded service names, service paths, events, endpoints, actions, action names, action paths, event details, and event triggers through the shared REST projection while preserving platform-owned JSON decoding and wrong-type behavior.
- Focused activity sample and sleep/time characterization now passes on iOS with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarActivityUtilsTests/testActivitySampleGoldenVectorsCoverStepAggregationAndSampleMapping -only-testing:PolarBleSdkTests/PolarSleepUtilsTests/testPartialNightGoldenVectorsPreserveIOSOmittedOptionalPolicy -only-testing:PolarBleSdkTests/PolarSleepUtilsTests/testReadSleepDataFromDayDirectory_SuccessfulResponse -only-testing:PolarBleSdkTests/PolarSleepUtilsTests/testSleepOffsetGoldenVectorsPreserveIOSPolicy -only-testing:PolarBleSdkTests/PolarTimeUtilsTests/testPbLocalDateTimeConversionToDate`; Android activity/sleep/time/nightly-recharge plus `GoldenVectorMigrationPolicyTest` pass with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarActivityUtilsTest' --tests 'com.polar.sdk.api.model.utils.PolarSleepUtilsTest' --tests 'com.polar.sdk.api.model.utils.PolarTimeUtilsTest' --tests 'com.polar.sdk.api.model.utils.PolarNightlyRechargeUtilsTest' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`, and the shared common policy slice passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.ActivitySummaryCommonPolicyTest' --tests 'com.polar.sharedtest.SleepNightlyRechargeCommonPolicyTest' --no-daemon --warning-mode all`.
- Focused backup restore-failure policy characterization now passes on iOS with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBackupManagerTest/testBackupGoldenVectorsPreserveIOSRestoreFailurePolicy`; Android backup plus `GoldenVectorMigrationPolicyTest` pass with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarBackupManagerTest' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`, and the shared backup utility policy slice passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.BackupUtilityCommonPolicyTest' --no-daemon --warning-mode all`.
- Backup workflow common coverage now includes a fake traversal/restore harness in `BackupUtilityCommonPolicyTest`, pinning `/SYS/` discovery, `BACKUP.TXT` expansion, directory read order, default user-file inclusion, restore PUT command order, and restore payload capture before broader backup traversal or restore execution moves further into shared KMP; platform restore failure aggregation remains a public compatibility gate.
- Backup workflow readiness now asserts exact policy-vector paths, exact policy IDs, exact required/covered behavior-family lists, and exact common migration decision text for BACKUP.TXT expansion, default user-file inclusion, restore PUT command order, payload bytes, restore failure aggregation policy, platform backup vector references, and compile verification instead of substring checks in shared common, Android, and iOS readiness consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.BackupUtilityCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarBackupManagerTest.backup workflow readiness manifest is pinned before workflow migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBackupManagerTest/testBackupWorkflowReadinessManifestIsPinnedBeforeWorkflowMigration -resultBundlePath /tmp/polar-ios-backup-readiness-exact.xcresult`.
- `BackupUtilityCommonPolicyTest.kt` now runs BACKUP.TXT expansion, backup file reads, restore PUT command planning, and restore-failure aggregation through production shared `PolarWorkflowRuntimePlanning` helpers and is guarded by `GoldenVectorMigrationPolicyTest.kt` plus `scripts/kmp_non_gradle_checks.rb` so default user-file inclusion, restore payload preservation, restore-failure platform split, restore-failure aggregation decision, and compile-verification terms cannot drift out of the shared common policy before backup workflow migration.
- Backup traversal root normalization now lives in shared `PolarWorkflowRuntimePlanning.backupTraversalRootPath`; Android and iOS backup traversal use that shared policy for `/U/*/` to `/U/0/` resolution while keeping directory listing, file reads/writes, restore failure aggregation, and public error behavior platform-owned.
- A refreshed full iOS XCTest run at `/tmp/polar-ios-full-refresh.xcresult` passes after clearing the final `PolarBleApiImplTests` facade fake-transport failures in low-level file APIs, stored-data cleanup, FTU sync/time ordering, daylight-saving timezone setup, and user-device-settings write-failure fixtures.
- Focused Android facade/vector validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; the Gradle 10 multi-string dependency deprecation warnings from AGP 8.13.0 have been removed by the AGP 9.2.1 upgrade and AGP 9 KMP plugin migration.
- Focused shared runtime policy validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FileFacadeRuntimePolicyCommonTest' --tests 'com.polar.sharedtest.StoredDataCleanupRuntimePolicyCommonTest' --tests 'com.polar.sharedtest.UserDeviceSettingsRuntimePolicyCommonTest' --tests 'com.polar.sharedtest.DiskTimeRuntimePolicyCommonTest' --no-daemon --warning-mode all`; it no longer reports AGP-internal Gradle deprecation warnings after the shared module moved to `com.android.kotlin.multiplatform.library`.
- User-device-settings runtime compatibility now runs `settings-runtime-policy.json` through production shared planning and `ScriptedCommonFakeTransport` in `UserDeviceSettingsRuntimePolicyCommonTest`, pinning facade-shaped read/write command capture, read-failure no-write behavior, and write-failure-after-payload behavior before any public settings facade delegation moves further into shared KMP; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.UserDeviceSettingsRuntimePolicyCommonTest' --no-daemon --warning-mode all`.
- iOS user-device-settings runtime field planning now uses shared KMP enum-name lookups for selected device-location, USB-connection-mode, and automatic-training-detection payload fields when `PolarBleSdkShared` is linked, preserving Swift enum-string fallbacks plus Swift-owned protobuf builders, filesystem capability gates, daylight-saving time-source behavior, write-failure-after-payload behavior, and public error mapping.
- Stored-data cleanup runtime compatibility now runs `cleanup-workflow-policy.json` through production shared planning and `ScriptedCommonFakeTransport` in `StoredDataCleanupRuntimePolicyCommonTest`, pinning facade-shaped GET/REMOVE command capture, list-failure no-remove behavior, and explicit empty-parent path split preservation before cleanup traversal moves further into shared KMP.
- Stored-data cleanup common coverage now also executes the cleanup policy through a fake traversal runtime in `StoredDataCleanupRuntimePolicyCommonTest`, deriving telemetry, SDLOGS, activity empty-parent, automatic-sample read/remove, list-failure, read-command, and remove-command order from scenario-level fake directory/file state before broader cleanup execution moves further into shared KMP; platform public error mapping and empty-parent path spelling remain adapter gates.
- Stored-data cleanup common production code now owns cutoff date comparison through `PolarWorkflowRuntimePlanning.storedDataDateIsOnOrBefore`; Android cleanup uses that shared policy for activity/date-folder and automatic-sample embedded-day delete decisions while keeping filesystem traversal, AUTOS protobuf parsing, removals, and public error behavior platform-owned.
- iOS cleanup now exposes the same shared cutoff date comparison through `PolarIosSharedBridge`, `PolarStoredDataOfflineRuntimePlanner`, and `PolarRuntimePlanner`; iOS production deletion uses it for date-folder and automatic-sample embedded-day decisions when `PolarBleSdkShared` is linked, with the existing Swift comparison as the SwiftPM/watchOS fallback and with filesystem traversal, AUTOS protobuf parsing, removals, and public error behavior still platform-owned.
- Stored-data cleanup directory-entry selection now delegates date-folder, data-type folder, SDLOGS extension, BPB inclusion, and USERID/HIST exclusion decisions through `PolarWorkflowRuntimePlanning.storedDataCleanupDirectoryEntryMatches`; Android and iOS facade predicates use the shared matcher while preserving platform-owned traversal, AUTOS protobuf parsing, removals, selected list-failure behavior, empty-parent path spelling, and SwiftPM/watchOS fallback checks.
- Android and linked iOS stored-data activity and automatic-sample removals now route selected file remove paths through shared cleanup operation planning after platform cutoff/AUTOS decisions, while filesystem traversal, AUTOS protobuf parsing, PSFTP execution, empty-parent deletion checks, SwiftPM/watchOS fallback, and public error behavior remain platform-owned.
- Reset/sync/H10 command runtime compatibility now runs `reset-sync-h10-command-policy.json` through production shared planning and `ScriptedCommonFakeTransport` in `CommandRuntimePolicyCommonTest`, pinning facade-shaped query/notification command capture, H10 query failure terminals, reset notification failure terminals, and sync failure platform-split preservation before public command delegation moves further into shared KMP.
- Disk/time runtime compatibility now runs `disk-time-query-policy.json` through production shared planning and `ScriptedCommonFakeTransport` in `DiskTimeRuntimePolicyCommonTest`, pinning facade-shaped query command capture, V2 system/local-time sequencing, H10 single-query behavior, and disk/local-time transport-error terminals while filesystem capability gates and public error mapping remain platform-owned.
- REST facade runtime compatibility now runs `rest-facade-runtime-policy.json` through production shared planning and `ScriptedCommonFakeTransport` in `RestFacadeRuntimePolicyCommonTest`, pinning facade-shaped GET request capture, request failure, response-error, empty-success parse failure, malformed-success parse failure, and success terminals before REST facade delegation moves further into shared KMP.
- File facade runtime compatibility now runs `file-facade-runtime-policy.json` through production shared planning and `ScriptedCommonFakeTransport` in `FileFacadeRuntimePolicyCommonTest`, pinning facade-shaped GET/PUT/REMOVE command capture, empty read success, request failures, response errors, write payload capture, write progress metadata, and write-stream failure after payload before low-level file facade delegation moves further into shared KMP.
- Offline-trigger runtime compatibility now runs `trigger-runtime-policy.json` through production shared planning and `ScriptedCommonFakeTransport` in `OfflineTriggerRuntimePolicyCommonTest`, pinning facade-shaped set-mode, status-read, and per-feature setting command capture plus control-point and transport-error terminals while byte-level PMD packet framing and response-queue cleanup remain platform-owned.
- Firmware workflow runtime compatibility now runs `workflow-runtime-policy.json` through production shared planning and a common fake dependency harness in `FirmwareWorkflowRuntimePolicyCommonTest`, pinning injectable fake network, package downloader, zip/filesystem, BLE writer, cleanup, retry-scheduler, artifact payload handoff, and per-file write-progress boundaries before production firmware workflow adapters move into shared KMP; platform facade retry policy, real artifact integration in production adapters, and public error mapping remain migration gates.
- Android and iOS firmware package payload selection now delegates the deterministic `readme.txt` exclusion to shared `PolarWorkflowRuntimePlanning.firmwarePackageEntryIsPayload`; zip byte extraction, BLE writes, progress emission, and public firmware workflow error mapping remain platform-owned until the firmware workflow facade gate is closed.
- Android and iOS firmware package payload name selection now delegates combined payload filtering plus write ordering to shared `PolarWorkflowRuntimePlanning.firmwarePayloadFileNames`; zip byte extraction, payload bytes, network download, and BLE writes remain platform-owned.
- Android and iOS firmware reboot-wait file selection now delegates the deterministic `SYSUPDAT.IMG` predicate to shared `PolarWorkflowRuntimePlanning.firmwareFileTriggersRebootWait`; Android/iOS PFTP write execution, reboot response acceptance, battery-too-low mapping, and public firmware workflow error mapping remain platform-owned until the firmware workflow facade gate is closed.
- Android and iOS firmware write terminal classification now delegates PFTP `REBOOTING` system-update success, non-system reboot propagation, and `BATTERY_TOO_LOW` terminal-failure identification to shared `PolarWorkflowRuntimePlanning.firmwareWriteTerminal`; platform adapters still own BLE write execution, concrete protobuf enum/error extraction, public error mapping, and status emission.
- Linked iOS firmware writes now pass platform-owned elapsed-time measurements into the shared time-gated write-progress emission helper through `PolarIosSharedBridge`, matching the Android shared planner coverage for percentage and max-interval decisions while actual progress scheduling and public status emission remain platform-owned.
- PSFTP timeout runtime compatibility now has a common fake harness in `PsFtpRuntimePolicyCommonTest`, pinning descriptor-enabled D2H observer setup, independent MTU and D2H channel capture, write acknowledgement counting, virtual-clock timeout advancement, scanner pause/resume cleanup, active observer cleanup, and no leaked pending operation for initial silence, continuation timeout, and write-ack timeout vectors; platform facade timeout compatibility and public error mapping remain migration gates before production PSFTP runtime moves further into shared KMP.
- The shared fake-transport base contract now has executable commonTest coverage and Android/iOS guard pins for scripted command outcomes, delayed responses, reconnect-after-failure controls, retry-delay scheduling, unscripted-operation timeouts, service readiness, virtual-clock timeout boundaries, stream cancellation cleanup, failed subscription behavior, disconnect-after-operation limits, command recording, and readiness-manifest completeness.
- The completed Android-hosted fake-transport contract is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so pre-KMP runtime tests keep read/write/subscribe/unsubscribe operations, bytes/response-error/transport-error/timeout/complete outcomes, hex payload capture, active-observer cleanup, failed-subscription no-registration, idempotent stream cancellation, and upstream-cancellation observation before runtime delegation.
- Fake-transport readiness and policy-vector guards now parse the shared fake-transport JSON on Android and iOS and assert exact vector IDs, input kinds, expected common decisions, expected outcomes/commands/polls/retry/check fields, policy-vector path order, required/covered behavior-family order, consumer test ownership lists, common runtime prototype status labels, wall-clock-safe execution flags, readiness common decision text, and readiness runtime-prototype metadata instead of broad string containment or sorted readiness equality; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FakeTransportContractCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.testutils.FakeTransportContractTest.fake transport readiness manifest is pinned before shared runtime delegation' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/StreamContinuationListTest/testFakeTransportReadinessManifestIsPinnedBeforeSharedRuntimeDelegation -resultBundlePath /tmp/polar-ios-fake-transport-readiness-order-exact.xcresult`.
- Low-level file facade runtime coverage now pins read, write, and delete response-error propagation in addition to non-empty and empty read success, read failure, write success with progress consumed before completion, write-stream failure after payload, delete success, delete request failure, command paths, payload capture, directory traversal references, and shared common runtime planning.
- REST facade runtime coverage now pins service-list and service-description response-error platform mapping plus service-description request-failure propagation and empty-success/malformed-success parse/decode failures in addition to service-list request failure, service-list success, service-description success, request paths, decoded fields, model JSON mapping references, and shared common request planning.
- Focused REST empty-success facade validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.listRestApiServices empty successful response is parse failure' --tests 'com.polar.sdk.impl.BDBleApiImplTest.getRestApiDescription empty successful response is parse failure' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' :shared:jvmTest --tests 'com.polar.sharedtest.RestFacadeRuntimePolicyCommonTest' --no-daemon --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_getRestApiDescription_emptySuccessfulResponse_failsDecoding -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_listRestApiServices_emptySuccessfulResponse_failsDecoding -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testRestFacadeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-rest-empty-success.xcresult`.
- Command runtime coverage now pins H10 recording start/stop/status query failure propagation and every reset-style notification failure propagation in addition to reset flag payloads, H10 recording start/stop/status queries, sync start/stop sequencing, and sync platform-split failure behavior.
- User-device-settings runtime coverage now pins device-location write-failure-after-payload behavior in the golden-vector policy, readiness manifest, shared commonTest policy, Android facade characterization, and iOS facade characterization; the focused Android facade rerun passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'BDBleApiImplTest' --no-daemon --warning-mode all`.
- Disk/time runtime coverage now pins local-time and local-time-with-zone query failure propagation in addition to disk-space query failure, set-local-time failure, V2 system/local-time sequencing, H10 single-query behavior, query IDs, null query parameters, and shared common runtime planning.
- Generic stream runtime coverage now pins disconnect-after-subscription cleanup and terminal mapping through `disconnect-after-subscription-policy.json`, `StreamRuntimePolicyCommonTest.kt`, Android `ChannelUtilsTests.kt`, and iOS `StreamContinuationListTest.swift`; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.common.ble.ChannelUtilsTests' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' :shared:jvmTest --tests 'com.polar.sharedtest.StreamRuntimePolicyCommonTest' --no-daemon --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/StreamContinuationListTest/testDisconnectAfterSubscription_finishesWithGattDisconnectedAndRemovesEntry -only-testing:iOSCommunicationsTests/StreamContinuationListTest/testStreamRuntimeReadinessManifestIsPinnedBeforeStreamRuntimeMigration -resultBundlePath /tmp/polar-ios-stream-disconnect.xcresult`.
- Generic stream runtime edge coverage now pins post-cancellation late-event suppression through `consumer-cancellation-late-events-policy.json`, `StreamRuntimePolicyCommonTest.kt`, Android `ChannelUtilsTests.kt`, and iOS `StreamContinuationListTest.swift`, covering late emissions, late terminal errors, and late completion signals after consumer cancellation without mutating terminal counters or re-emitting values.
- Generic stream runtime policy-vector and readiness guards now parse stream-runtime JSON and assert exact vector IDs, input kinds, execution kind/transport, wall-clock-safe flags, common decisions, policy-vector path order, required/covered behavior-family order, consumer ownership lists, readiness common decision text, and readiness runtime-prototype metadata instead of raw vector text containment, sorted readiness equality, or readiness decision substring checks in shared common, Android, and iOS stream tests; `scripts/kmp_non_gradle_checks.rb` now mirrors the `StreamRuntimePolicyCommonTest.kt` required terms so ordered emissions, terminal errors, connection guards, unchecked subscription, consumer cancellation, disconnect cleanup, duplicate completion, post-completion suppression, platform vector references, and compile-verification terms fail in the cheap pre-Gradle guard if they drift; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.StreamRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.common.ble.ChannelUtilsTests.stream runtime readiness manifest is pinned before stream runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/StreamContinuationListTest/testStreamRuntimeReadinessManifestIsPinnedBeforeStreamRuntimeMigration -resultBundlePath /tmp/polar-ios-stream-runtime-readiness-order-exact.xcresult`.
- Shared common readiness gates now use structured manifest `id` assertions and exact platform-flag assertions instead of broad manifest string containment for the runtime/model gates touched in this coverage batch, including command, disk/time, stored-data cleanup, firmware workflow, D2H stream/mapping, REST facade/request/compression, file facade/runtime, user-device-settings runtime, offline-trigger runtime, backup workflow, PSFTP byte-codec/runtime, disk-space, watch-face, KVTX, generic stream runtime, PMD secret, and ECG parser readiness; `scripts/kmp_non_gradle_checks.rb` and `GoldenVectorMigrationPolicyTest.kt` now require shared common readiness consumers to pin the manifest `platforms.android`, `platforms.ios`, and `platforms.common` flags; focused validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.DiskSpaceCommonPolicyTest' --tests 'com.polar.sharedtest.CommandRuntimePolicyCommonTest' --tests 'com.polar.sharedtest.FileFacadeRuntimePolicyCommonTest' --tests 'com.polar.sharedtest.RestRequestTransportPolicyCommonTest' --tests 'com.polar.sharedtest.StoredDataCleanupRuntimePolicyCommonTest' --tests 'com.polar.sharedtest.WatchFaceCommonPolicyTest' --tests 'com.polar.sharedtest.PmdSecretCommonPolicyTest' --tests 'com.polar.sharedtest.EcgParserCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest.shared common readiness consumers explicitly pin platform flags' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- The cheap Ruby mirror now directly guards shared fake transport, REST service mapping, REST event compression, file facade runtime, PSFTP runtime, and generic stream runtime common-test required terms, and self-audits every `FAKE_TRANSPORT_COMMON_*_TEST_REQUIRED_TERMS` list against `GoldenVectorMigrationPolicyTest.kt` for exact name/content/order parity, so command/outcome primitives, service-readiness/virtual-clock behavior, REST model mapping and codec ownership policy, low-level file command planning, PSFTP timeout/error/progress behavior, stream cancellation/cleanup behavior, consumer ownership, platform-reference gates, and compile-verification terms fail before Gradle if they drift out of the shared common policy tests or if a future Kotlin policy list lacks a Ruby mirror.
- The non-Gradle policy parser now self-audits every Kotlin `*_COMMON_POLICY_REQUIRED_TERMS` target-file check, including D2H stream runtime, so parser/model/domain common policy guards must resolve to concrete shared common test files before the Ruby gate can pass; this keeps future parser/model golden-vector policy lists from bypassing the cheap pre-Gradle guard when `GoldenVectorMigrationPolicyTest.kt` changes.
- The non-Gradle parser also consumes Kotlin validation/probe/TDD/shared-consumption term lists that use either `_TERMS` or `_REQUIRED_TERMS`, so non-Gradle metadata gates, hardware-smoke boundaries, Gradle batching guidance, TDD golden-vector example fields, TDD minimum-validation/regression/coverage/first-slice guidance, stale TDD vector-example terms, iOS Xcode probe expectations, full XCTest execution requirements, remaining-work XCTest requirements, and shared artifact consumption requirements cannot drift in `GoldenVectorMigrationPolicyTest.kt` without the cheap Ruby gate checking their documented targets.
- The Ruby mirror now also parses Kotlin map-based ledger and migration-boundary constants for facade-gate-open rows, runtime-pinned facade rows, PSFTP timeout rows, byte-level common dependency deferrals, and platform-owned coverage rows, then compares them against the Ruby maps for exact parity, so future changes to `FACADE_GATE_OPEN_REQUIRED_TERMS`, `RUNTIME_PINNED_FACADE_LEDGER_REQUIRED_TERMS`, `PSFTP_TIMEOUT_LEDGER_REQUIRED_TERMS`, `BYTE_LEVEL_COMMON_DEPENDENCY_DEFERRAL_TERMS`, or `PLATFORM_OWNED_COVERAGE_ROWS` cannot bypass the pre-Gradle fake-transport, codec-deferral, or platform-ownership guards.
- The Kotlin term-list parser now handles single-line and multi-line `listOf(...)` constants and uses `MIGRATION_README_TERMS` from `GoldenVectorMigrationPolicyTest.kt` for fixture README migration-context checks, so the cheap Ruby gate no longer hardcodes the accepted common/shared/migration wording for golden-vector README ownership notes.
- Android and iOS public facade readiness guards now parse command, stored-data cleanup, disk/time, user-device-settings, REST facade, offline-trigger, and file facade runtime readiness manifests as JSON and assert exact manifest ids, kinds, single policy-vector paths, required behavior-family sets, covered behavior-family sets, and compile-verified migration decisions instead of broad string containment; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.command runtime readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.impl.BDBleApiImplTest.stored data cleanup readiness manifest is pinned before cleanup migration' --tests 'com.polar.sdk.impl.BDBleApiImplTest.disk time readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.impl.BDBleApiImplTest.user device settings readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.impl.BDBleApiImplTest.rest facade readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.impl.BDBleApiImplTest.offline trigger runtime readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.impl.BDBleApiImplTest.file facade readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testCommandRuntimeReadinessManifestIsPinnedBeforeRuntimeMigration -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testStoredDataCleanupReadinessManifestIsPinnedBeforeCleanupMigration -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testDiskTimeReadinessManifestIsPinnedBeforeRuntimeMigration -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testUserDeviceSettingsReadinessManifestIsPinnedBeforeRuntimeMigration -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testRestFacadeReadinessManifestIsPinnedBeforeRuntimeMigration -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testOfflineTriggerRuntimeReadinessManifestIsPinnedBeforeRuntimeMigration -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testFileFacadeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-facade-readiness-structured.xcresult`; both warning/error scans are empty.
- Android and iOS PSFTP byte-codec plus disk-space readiness guards now assert exact policy-vector path lists and exact required/covered behavior-family sets, closing subset-only manifest checks for RFC76/RFC60 byte-codec coverage and disk-space unsigned-fragment policy coverage; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.psftp.BlePsFtpUtilsTest.psftp byte codec readiness manifest is pinned before codec migration' --tests 'com.polar.sdk.api.model.PolarDiskSpaceTest.disk space readiness manifest is pinned before model migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/BlePsFtpUtilityTest/testPsFtpByteCodecReadinessManifestIsPinnedBeforeCodecMigration -only-testing:PolarBleSdkTests/PolarDiskSpaceDataTest/testDiskSpaceReadinessManifestIsPinnedBeforeModelMigration -resultBundlePath /tmp/polar-ios-psftp-disk-readiness-exact.xcresult`; both warning/error scans are empty after cleaning incidental iOS test warnings in advertisement, PPG, and PSFTP client tests.
- PSFTP byte-codec readiness guards now assert exact RFC76/RFC60 policy-vector path order, exact required/covered behavior-family order, exact common migration decision text, and Android/iOS/common consumer ownership metadata across shared common, Android, and iOS tests instead of sorted equality and decision substring checks for header next/status/sequence/payload decoding, RFC76 error-frame platform split, complete-message stream encoding, Android file-data append behavior, iOS request write frame splitting, MTU frame splitting, sequence wrap, and compile verification; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PsFtpByteCodecCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.psftp.BlePsFtpUtilsTest.psftp byte codec readiness manifest is pinned before codec migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/BlePsFtpUtilityTest/testPsFtpByteCodecReadinessManifestIsPinnedBeforeCodecMigration -resultBundlePath /tmp/polar-ios-psftp-byte-codec-readiness-common-exact.xcresult`.
- Shared PSFTP byte-codec coverage now asserts exact RFC76 vector IDs/cases, exact common consumer/platform ownership for every byte-codec policy vector, exact Android request file-data append decision and platform split, exact iOS request write frame splitting, and exact readiness policy-vector ID/case order instead of broad `platforms` presence or common-decision substring checks in `PsFtpByteCodecCommonPolicyTest.kt`.
- PSFTP byte-codec case coverage now pins exact RFC60 complete-message case IDs and RFC76 frame-splitting case IDs in shared common, Android, iOS, Ruby, and Gradle guards instead of only executing the vector cases implicitly; validation passes with the PSFTP byte-codec case-reference audit, `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PsFtpByteCodecCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.psftp.BlePsFtpUtilsTest' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`, and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,id=0DB7D4B5-705D-4195-8F51-75425EBF2B96,arch=arm64' -only-testing:iOSCommunicationsTests/BlePsFtpUtilityTest/testMessageStreamGoldenVectorsEncodeRfc60Messages -only-testing:iOSCommunicationsTests/BlePsFtpUtilityTest/testMessageStreamGoldenVectorsSplitRfc76Frames -resultBundlePath /tmp/polar-ios-psftp-byte-codec-case-pins.xcresult`.
- PSFTP runtime readiness guards now assert exact policy-vector path order, exact required/covered behavior-family order, exact common migration decision text, exact shared runtime-prototype metadata, and Android/iOS/common consumer ownership metadata across shared common, Android, and iOS tests instead of set equality and decision substring checks for request-response reassembly, response-error mapping, notification ordering, initial-silence policy, consumer timeout cleanup, notification error platform split, continuation timeout, write progress split, write interruption, transport failure, write acknowledgement timeout, fake-clock timeout gates, and compile verification; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PsFtpRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.psftp.BlePsFtpClientTest.psftp runtime readiness manifest is pinned before shared runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/BlePsFtpClientTest/testPsFtpRuntimeReadinessManifestIsPinnedBeforeSharedRuntimeMigration -resultBundlePath /tmp/polar-ios-psftp-runtime-readiness-exact.xcresult`.
- Shared PSFTP runtime policy coverage now asserts exact notification error notes/runtime prototype, write interruption/transport/ack policy ids/cases/kinds, exact error-policy decisions, exact fake-runtime or timeout execution metadata, exact write-progress notes, exact Android/iOS/common consumer ownership, platform flags, and readiness policy-vector id/case order instead of substring checks in `PsFtpRuntimePolicyCommonTest.kt`.
- Shared PSFTP runtime case coverage now pins exact request-response, notification reassembly, ordering, initial-silence, notification error, and continuation-timeout case IDs in shared common, Ruby, and Android-hosted Gradle guards instead of only iterating vector cases implicitly; validation passes with the PSFTP runtime case-reference audit, `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, and from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PsFtpRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- Android and iOS PSFTP platform/runtime vector consumers now also pin exact request-response, response-error, notification reassembly, notification ordering, notification error, and continuation-timeout case IDs in `BlePsFtpClientTest.kt`, shared `PsFtpRuntimePolicyCommonTest.kt`, and `BlePsFtpClientTest.swift` instead of only executing vector loops; the retired Android-hosted `PsFtpCommonFakeRuntimeTest.kt` prototype has been removed, and validation passes with the PSFTP platform case-reference audit, `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PsFtpRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.psftp.BlePsFtpClientTest' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`, and focused iOS XCTest with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/BlePsFtpClientTest/testPsFtpNotificationGoldenVectorsReassembleCompleteNotifications -only-testing:iOSCommunicationsTests/BlePsFtpClientTest/testPsFtpNotificationGoldenVectorsPreserveCompleteNotificationOrdering -only-testing:iOSCommunicationsTests/BlePsFtpClientTest/testPsFtpResponseGoldenVectorsReassembleRequestResponses -only-testing:iOSCommunicationsTests/BlePsFtpClientTest/testPsFtpResponseGoldenVectorsCharacterizeRequestErrorPolicy -only-testing:iOSCommunicationsTests/BlePsFtpClientTest/testPsFtpNotificationGoldenVectorsCharacterizeErrorPolicy -only-testing:iOSCommunicationsTests/BlePsFtpClientTest/testPsFtpTimeoutPlanningVectorsRequireFakeClockBeforeSharedRuntimeMigration -resultBundlePath /tmp/polar-ios-psftp-platform-case-pins.xcresult`.
- Disk-space readiness guards now assert exact policy-vector path order, exact required/covered behavior-family order, exact common migration decision text, and Android/iOS/common consumer ownership metadata across shared common, Android, and iOS tests, and the shared `DiskSpaceCommonPolicyTest.kt` decision literal is now contiguous so the repository-wide exact common-decision audit can prove coverage without string-reconstruction heuristics; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.DiskSpaceCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.PolarDiskSpaceTest.disk space readiness manifest is pinned before model migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDiskSpaceDataTest/testDiskSpaceReadinessManifestIsPinnedBeforeModelMigration -resultBundlePath /tmp/polar-ios-disk-space-readiness-order-exact.xcresult`.
- Android and iOS PMD control-point plus active-measurement readiness guards now assert exact policy-vector path lists, exact manifest kind, and exact required/covered behavior-family sets, closing subset-only manifest checks for settings responses, measurement-status responses, unknown measurement type policy, platform vector-reference gates, and active-measurement state decoding; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.BlePmdClientControlPointResponseTest.PMD control point readiness manifest is pinned before response parser migration' --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.PmdActiveMeasurementTest.PMD control point readiness manifest pins active measurement coverage before migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/BlePmdClientTest/testPmdControlPointReadinessManifestIsPinnedBeforeResponseParserMigration -only-testing:iOSCommunicationsTests/PmdActiveMeasurementTest/testPmdControlPointReadinessManifestPinsActiveMeasurementCoverageBeforeMigration -resultBundlePath /tmp/polar-ios-pmd-control-readiness-exact.xcresult`; both warning/error scans are empty.
- Android PMD client feature-to-public data-type mapping now classifies measurement types through shared `PolarPmdMeasurementTypeName` before applying the Android public enum adapter, preserving the MAG/MAGNETOMETER naming boundary and the existing unknown-type error mapping.
- Android, iOS, and shared common PMD settings readiness guards now assert exact policy-vector path order, exact manifest kind, exact required/covered behavior-family order, exact readiness common decision text, and Android/iOS/common consumer ownership instead of set equality, broad manifest substring checks, or missing platform consumer assertions for FACTOR parsing, skipped FACTOR serialization, RANGE_MILLIUNIT signedness platform policy, SECURITY setting parse policy, truncated-value platform policy, unknown setting type platform policy, and platform vector-reference gates; focused Android/shared validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PmdSettingsCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.PmdSettingTest.PMD settings readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/PmdSettingTest/testPmdSettingsReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-pmd-settings-readiness-order-exact.xcresult`.
- Android, iOS, and shared common PMD secret readiness guards now assert exact policy-vector path order, exact manifest kind, exact required/covered behavior-family order, exact readiness common decision text, exact invalid-key and unknown-strategy common decision strings, and Android/iOS/common consumer ownership instead of set equality, substring decision checks, or platform presence-only checks for SECURITY setting serialization, NONE/XOR/AES key validation, NONE/XOR decryption policy, AES block-alignment gates, and platform vector-reference gates; focused Android/shared validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PmdSecretCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.PmdSecretTest.PMD secret readiness manifest is pinned before secret strategy migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/PmdSecretTest/testPmdSecretReadinessManifestIsPinnedBeforeSecretStrategyMigration -resultBundlePath /tmp/polar-ios-pmd-secret-readiness-order-exact.xcresult`.
- Android and iOS ACC parser readiness guards now assert exact policy-vector path lists, exact manifest kind, and exact required/covered behavior-family sets, closing subset-only manifest checks for raw type 1 parsing and timestamp interpolation, compressed type 1 delta decoding, unsupported compressed type policy, truncated raw-sample policy, truncated compressed-delta header policy, and platform vector-reference gates; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.AccDataTest.ACC readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/AccDataTest/testAccReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-acc-readiness-exact.xcresult`; both warning/error scans are empty.
- Android and iOS ECG parser readiness guards now assert exact policy-vector path lists, exact manifest kind, and exact required/covered behavior-family sets, closing subset-only manifest checks for raw type 0 boundary values, raw type 0 timestamp interpolation, Android-owned raw type 1 status bits, Android-owned raw type 2 tags, and platform vector-reference gates; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.EcgDataTest.ECG readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/EcgDataTest/testEcgReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-ecg-readiness-exact.xcresult`; both warning/error scans are empty.
- Android and iOS GYR parser readiness guards now assert exact policy-vector path lists, exact manifest kind, and exact required/covered behavior-family sets, closing subset-only manifest checks for compressed type 0 timestamp interpolation, unsupported raw-frame policy, unsupported compressed-frame policy, truncated compressed-delta header policy, and platform vector-reference gates; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.GyrDataTest.GYR readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/GyrDataTest/testGyrReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-gyr-readiness-exact.xcresult`; both warning/error scans are empty.
- Android and iOS MAG parser readiness guards now assert exact policy-vector path lists, exact manifest kind, and exact required/covered behavior-family sets, closing subset-only manifest checks for compressed type 0 factor scaling, compressed type 0 timestamp interpolation, unsupported raw-frame policy, unsupported compressed-frame policy, truncated compressed-delta header policy, and platform vector-reference gates; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.MagDataTest.MAG readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/MagDataTest/testMagReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-mag-readiness-exact.xcresult`; both warning/error scans are empty.
- Android and iOS offline HR parser readiness guards now assert exact policy-vector path lists, exact manifest kind, and exact required/covered behavior-family sets, closing subset-only manifest checks for unsupported compressed-frame policy, unsupported raw-frame policy, and platform vector-reference gates; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.OfflineHrDataTest.offline HR readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/OfflineHrDataTest/testOfflineHrReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-offline-hr-readiness-exact.xcresult`; both warning/error scans are empty.
- Android and iOS PPI parser readiness guards now assert exact policy-vector path lists, exact manifest kind, and exact required/covered behavior-family sets, closing subset-only manifest checks for unsupported compressed-frame policy, unsupported raw-frame policy, and platform vector-reference gates; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.PpiDataTest.PPI readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/PpiDataTest/testPpiReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-ppi-readiness-exact.xcresult`; both warning/error scans are empty.
- Android and iOS skin-temperature parser readiness guards now assert exact policy-vector path lists, exact manifest kind, and exact required/covered behavior-family sets, closing subset-only manifest checks for unsupported raw-frame policy and platform vector-reference gates; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.SkinTemperatureDataTest.skin temperature readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/SkinTemperatureDataTest/testSkinTemperatureReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-skin-temperature-readiness-exact.xcresult`; both warning/error scans are empty.
- Android and iOS pressure/temperature parser readiness guards now assert exact policy-vector path lists, exact manifest kind, and exact required/covered behavior-family sets, closing subset-only manifest checks for negative and boundary float values, raw type 0 timestamp interpolation, unsupported raw/compressed frame policies, truncated raw-sample policy, and platform vector-reference gates; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.TemperatureDataTest.pressure temperature readiness manifest is pinned before scalar parser migration' --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.PressureDataTest.pressure temperature readiness manifest is pinned before scalar parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/TemperatureDataTest/testPressureTemperatureReadinessManifestIsPinnedBeforeScalarParserMigration -resultBundlePath /tmp/polar-ios-pressure-temperature-readiness-exact.xcresult`; both warning/error scans are empty.
- Android and iOS PPG frame-family readiness guards now assert exact manifest id, required vector path set, required/covered family set, and common migration decision text while still cross-checking the manifest path set against executable PPG vector files, closing subset-only checks for all raw, compressed, unsupported, malformed, and platform-split PPG parser families; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.PpgDataTest.ppgFrameFamilyReadinessManifestPinsAndroidMigrationGate' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/PpgDataTest/testPpgFrameFamilyReadinessManifestPinsIOSMigrationGate -resultBundlePath /tmp/polar-ios-ppg-readiness-exact.xcresult`; both warning/error scans are empty.
- Android and iOS device-ID readiness guards now assert exact policy-vector path lists, exact manifest kind, and exact required/covered behavior-family sets across checksum assembly, validation, UUID conversion, identifier routing, platform split decisions, and vector-reference gates; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.polar.DeviceIdGoldenVectorTest.device ID readiness manifest is pinned before checksum and UUID migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/BlePolarDeviceIdUtilityTest/testDeviceIdReadinessManifestIsPinnedBeforeChecksumMigration -only-testing:PolarBleSdkTests/PolarDeviceUuidTest/testDeviceIdReadinessManifestIsPinnedBeforeUuidMigration -resultBundlePath /tmp/polar-ios-device-id-readiness-exact.xcresult`; both warning/error scans are empty.
- The full Android library unit gate is unblocked after aligning the PSFTP fake-runtime progress-policy assertion with `write-success-progress.json`; `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --no-daemon --warning-mode all` now passes from `sources/Android/android-communications` with no Gradle warning output, and the full iOS simulator XCTest gate passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -resultBundlePath /tmp/polar-ios-xctest-unblock-current.xcresult`.
- Android and iOS TypeUtils readiness guards now assert the exact policy-vector path list, exact manifest kind, exact required/covered behavior-family list, exact common migration decision text, and Android/iOS/common consumer ownership for unsigned byte/int/long conversion, signed sign extension, offset and size selection, signed-minimum boundaries, high-bit unsigned platform decisions, empty payload and payload-too-long typed errors, UInt64 max decimal preservation, platform vector references, and compile verification; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.common.ble.TypeUtilsTest.type utils readiness manifest is pinned before parser primitive migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/TypeUtilsTest/testTypeUtilsReadinessManifestIsPinnedBeforeParserPrimitiveMigration -resultBundlePath /tmp/polar-ios-type-utils-ownership-pins.xcresult`; Swift parse, Ruby guardrails, and whitespace checks are clean, and the ownership audit drops the TypeUtils rows.
- Shared TypeUtils readiness coverage now asserts exact readiness vector area/case, policy-vector path order, required/covered behavior-family order, common migration decision, Android/iOS/common consumer ownership, and platform flags instead of membership loops in `TypeUtilsCommonPolicyTest.kt`, and the decision literal is now contiguous so the repository-wide exact common-decision audit can prove `UInt64` policy coverage without string-reconstruction heuristics.
- Android, iOS, and shared common advertisement readiness guards now assert exact policy-vector path order, exact manifest kind, exact required/covered behavior-family order, exact common migration decision text, and Android/iOS/common consumer ownership instead of set equality, manifest substring checks, or missing platform consumer assertions for Polar/custom/seven-digit local-name parsing, non-Polar local-name platform decisions, manufacturer HR presence/absence, non-Polar/unknown company behavior, unknown Polar segment handling, malformed GPB and truncated HR-candidate policies, service UUID membership, RSSI median calculation, platform vector references, and compile verification; focused Android/shared validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.AdvertisementCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.advertisement.BleAdvertisementContentTest.advertisement readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/BleAdvertisementContentTest/testAdvertisementReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-advertisement-readiness-order-exact.xcresult`.
- Android activity-data feature readiness now derives the capability lookup device type from shared `PolarAdvertisementModels.deviceModelNameFromLocalName` when the advertised local name is a valid Polar name, while preserving the previous nonstandard-name fallback and keeping the actual `BlePolarDeviceCapabilitiesUtility.isActivityDataSupported` gate platform-owned.
- Android and iOS skin-temperature domain readiness guards now assert the exact policy-vector path list, exact manifest kind, exact required/covered behavior-family list, common migration decision text, and Android/iOS/common consumer ownership for sourceDeviceId ownership, empty sample lists, sample delta/temperature preservation, measurement and sensor-location mappings, unknown enum typed-boundary behavior, platform vector references, and compile verification; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarSkinTemperatureUtilsTest.skin temperature domain readiness manifest is pinned before model migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarSkinTemperatureUnitTest/testSkinTemperatureDomainReadinessManifestIsPinnedBeforeModelMigration -resultBundlePath /tmp/polar-ios-skin-temperature-domain-readiness-exact.xcresult`; Swift parse, Ruby guardrails, and whitespace checks are clean.
- Shared skin-temperature domain readiness coverage now asserts exact readiness vector area/case, policy-vector path order, required/covered behavior-family order, common migration decision, Android/iOS/common consumer ownership, and platform flags instead of membership loops in `SkinTemperatureDomainCommonPolicyTest.kt`.
- Android, iOS, and shared common user-device-settings model readiness guards now assert exact policy-vector path order, exact migration-readiness state, exact required/covered behavior-family order, and Android/iOS/common consumer ownership instead of set equality or platform presence-only checks for protobuf presence preservation, nullable omitted optional settings, writable settings serialization, encoder-owned trusted last-modified values, explicit telemetry write policy, platform default divergence, platform vector references, and compile verification; focused Android/shared validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.UserDeviceSettingsCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.PolarUserDeviceSettingsTest.user device settings model readiness manifest is pinned before model migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarUserDeviceSettingsUtilsTest/testUserDeviceSettingsModelReadinessManifestIsPinnedBeforeModelMigration -resultBundlePath /tmp/polar-ios-user-device-settings-model-readiness-order-exact.xcresult`.
- User-device-settings common production code now owns the presence-preserving nullable field model and serialized write-field plan in `PolarUserDeviceSettingsModels`, including trusted last-modified metadata and explicit telemetry write policy, while platform protobuf byte parsing/building and public default compatibility remain Android/iOS adapter gates.
- Android `PolarUserDeviceSettings.fromBytes` now normalizes protobuf-decoded field presence through shared `PolarUserDeviceSettingsModels.parsePresencePreservingFields`, then reapplies Android public default compatibility for omitted numeric training fields and AUTOS state; Android `toProto` now consumes the shared serialized write-field plan for device location, USB mode, automatic-training settings, and AUTOS state while preserving Android's existing telemetry omission adapter behavior.
- User-device-settings model readiness now records `compileVerifiedPreMigrationCharacterization` instead of `blockedUntilCompileVerified`; the refreshed focused Gradle validation passed with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.UserDeviceSettingsCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.PolarUserDeviceSettingsTest.user device settings model readiness manifest is pinned before model migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`, and the corrected iOS XCTest selector `xcodebuild test -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme PolarBleSdk -destination 'platform=iOS Simulator,id=0DB7D4B5-705D-4195-8F51-75425EBF2B96,arch=arm64' -only-testing:PolarBleSdkTests/PolarUserDeviceSettingsUtilsTest/testUserDeviceSettingsModelReadinessManifestIsPinnedBeforeModelMigration` executed one test.
- Android and iOS activity, automatic-sample, and daily-summary readiness guards now assert the exact policy-vector path list, exact manifest kind, exact required/covered behavior-family list, common migration decision text, and Android/iOS/common consumer ownership for activity request paths, aggregation, intervals, activity-info projection, malformed activity-sample behavior, automatic HR trigger and heart-rate arrays, PPI delta/status decoding, daily-summary path/scalar/duration projection, platform vector references, and compile verification; the Android activity test warning cleanup removes unnecessary safe-call and non-null assertion compiler warnings in the touched Gradle gate; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarAutomaticSamplesUtilsTest.activity summary readiness manifest is pinned before automatic sample migration' --tests 'com.polar.sdk.api.model.utils.PolarActivityUtilsTest.activity summary readiness manifest is pinned before model migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarAutomaticSamplesUnitTest/testActivitySummaryReadinessManifestIsPinnedBeforeAutomaticSampleMigration -only-testing:PolarBleSdkTests/PolarActivityUtilsTest/testActivitySummaryReadinessManifestIsPinnedBeforeModelMigration -resultBundlePath /tmp/polar-ios-activity-summary-readiness-exact.xcresult`; Swift parse, Ruby guardrails, and whitespace checks are clean.
- Shared activity summary coverage now asserts exact malformed activity-sample vector id/area/case, request date/path, file paths, Android empty/default behavior, iOS throw behavior, consumer ownership, platform flags, notes, exact readiness policy-vector order, exact required/covered family order, exact readiness common decision, and exact readiness consumer ownership instead of substring and membership checks in `ActivitySummaryCommonPolicyTest.kt`.
- Android and iOS offline-recording metadata readiness guards now assert the exact policy-vector path list, exact manifest kind, exact required/covered behavior-family list, common migration decision text, and Android/iOS/common consumer ownership for filename classification, split-file normalization, invalid filename handling, PMDFILES grouping, zero-size and invalid-entry filtering, representative path policy, trigger model projection, disabled-trigger filtering, platform vector references, and compile verification; the Android offline-recording test warning cleanup removes always-true nullable-date assertions in the touched Gradle gate; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarOfflineRecordingUtilsTest.offline recording metadata readiness manifest is pinned before migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarOfflineRecordingUtilsTest/testOfflineRecordingMetadataReadinessManifestIsPinnedBeforeMigration -resultBundlePath /tmp/polar-ios-offline-recording-metadata-readiness-exact.xcresult`; Swift parse, Ruby guardrails, and whitespace checks are clean.
- Shared offline-recording metadata readiness coverage now asserts exact readiness vector area/case, policy-vector path order, required/covered behavior-family order, common migration decision, Android/iOS/common consumer ownership, and platform flags instead of membership loops in `OfflineRecordingMetadataCommonPolicyTest.kt`.
- All declared offline-recording metadata platform consumers now pin the exact metadata readiness behavior-family order, including `OfflineRecordingUtilityTest`, `PolarDataUtilsTest`, `OfflineRecordingUtilsTest`, and the existing facade-level `PolarOfflineRecordingUtilsTest`; the repository-wide readiness-family mirror audit now returns `TOTAL=0`, and focused validation passes with `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, `swiftc -parse sources/iOS/ios-communications/Tests/iOSCommunicationsTests/OfflineRecordingUtilsTest.swift sources/iOS/ios-communications/Tests/PolarBleSdkTests/PolarDataUtilsTest.swift`, Android Gradle for the two new metadata readiness tests plus `GoldenVectorMigrationPolicyTest`, and focused iOS XCTest for `OfflineRecordingUtilsTest/testOfflineRecordingMetadataReadinessManifestIsPinnedBeforeMetadataMigration` plus `PolarDataUtilsTest/testOfflineRecordingMetadataReadinessManifestIsPinnedBeforeMetadataMigration`.
- Android and iOS SPo2 model readiness guards now assert the exact policy-vector path list, exact manifest kind, exact required/covered behavior-family list, common migration decision text, and Android/iOS/common consumer ownership for passed-normal field mapping, optional protobuf presence preservation, empty recording-device normalization, nullable triggerType policy, Android/iOS trigger-field platform references, unknown SPo2 class typed-boundary behavior, platform vector references, and compile verification; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarTestUtilsTest.spo2 readiness manifest is pinned before model migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarTestUtilsTest/testSpo2ReadinessManifestIsPinnedBeforeModelMigration -resultBundlePath /tmp/polar-ios-spo2-readiness-exact.xcresult`; Swift parse, Ruby guardrails, and whitespace checks are clean.
- Android SPo2 test folder timestamp formatting now delegates through `PolarRuntimePlannerAdapter.spo2TestTimeFromFolderNames` to shared `PolarSpo2Models.testTimeFromFolderNames`, preserving Android PSFTP transport, protobuf parsing, public model mapping, and proto `testTime` fallback in the platform adapter; the platform adapter test now pins both valid and invalid shared timestamp decisions.
- Shared SPo2 readiness coverage now asserts exact readiness vector area/case, policy-vector path order, required/covered behavior-family order, common migration decision, Android/iOS/common consumer ownership, and platform flags instead of membership loops in `Spo2CommonPolicyTest.kt`.
- Android and iOS training-session readiness guards now assert the exact policy-vector path list, exact manifest kind, exact required/covered behavior-family list, and common migration decision text for directory traversal, summary discovery, exercise classification, unknown-file ignoring, aggregate size, exercise path policy, missing exercise-file policy, payload fetch order, progress calculation, malformed component isolation, unknown advanced sample-list handling, known sample preservation, parser-family ownership, byte-level parser dependency gates, platform vector references, and compile verification; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarTrainingSessionUtilsTest.training session readiness manifest is pinned before migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarTrainingSessionUtilsTest/testTrainingSessionReadinessManifestIsPinnedBeforeMigration -resultBundlePath /tmp/polar-ios-training-session-readiness-exact.xcresult`; Swift parse, Ruby guardrails, and whitespace checks are clean.
- Android and iOS training-session payload-parser policy guards now assert the exact parser case ID order, input/prototype parser names, encodings, expected fields, gzip-protobuf case count, common parser prototype status, exact byte-level parser dependency common decision, and Android/iOS/common consumer ownership metadata instead of substring and case-existence checks; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.TrainingSessionCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarTrainingSessionUtilsTest.payload parser policy vector is pinned before byte level parser migration' --tests 'com.polar.sdk.api.model.utils.PolarTrainingSessionUtilsTest.training session readiness manifest is pinned before migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarTrainingSessionUtilsTest/testPayloadParserPolicyVectorIsPinnedBeforeByteLevelParserMigration -only-testing:PolarBleSdkTests/PolarTrainingSessionUtilsTest/testTrainingSessionReadinessManifestIsPinnedBeforeMigration -resultBundlePath /tmp/polar-ios-training-session-payload-parser-exact.xcresult`.
- Training-session common production code now owns payload parser-family and encoding selection for every policy-vector file name through `PolarTrainingSessionModels.payloadParserCase`, while actual protobuf/gzip byte decoding remains blocked on the documented common dependency gate.
- Android and linked iOS training-session payload parsing now consume `PolarTrainingSessionModels.payloadParserCase` to select the platform protobuf parser and gzip/protobuf encoding family for exercise summary, route, advanced route, samples, and advanced samples payloads, while Android/iOS still own actual gzip inflation and protobuf byte decoding until common codec dependencies are approved.
- Shared training-session readiness coverage now asserts exact missing-exercise-file common policy text, readiness vector area/case, policy-vector path order, required/covered behavior-family order, common migration decision, Android/iOS/common consumer ownership, and platform flags instead of substring and membership checks in `TrainingSessionCommonPolicyTest.kt`.
- Android and iOS sleep/nightly recharge readiness guards now assert the exact policy-vector path list, exact manifest kind, exact required/covered behavior-family list, common migration decision text, and Android/iOS/common consumer ownership for nightly result date formatting, created/modified timestamp formatting, proto3 default preservation, malformed payload null policy, sleep end-offset, timezone-to-UTC instant policy, hypnogram order, cycle order, sleep stage enum mapping, partial-night optionals, platform vector references, and compile verification; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarNightlyRechargeUtilsTest.sleep nightly readiness manifest is pinned before model migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarNightlyRechargeUtilsTest/testSleepNightlyReadinessManifestIsPinnedBeforeModelMigration -resultBundlePath /tmp/polar-ios-sleep-nightly-readiness-exact.xcresult`; Swift parse, Ruby guardrails, and whitespace checks are clean.
- Shared sleep/nightly recharge coverage now asserts exact partial-night and sleep-offset common policy decisions, readiness vector area/case, policy-vector path order, required/covered behavior-family order, common migration decision, Android/iOS/common consumer ownership, and platform flags instead of substring and membership checks in `SleepNightlyRechargeCommonPolicyTest.kt`.
- Android and iOS D2H notification readiness guards now assert exact stream-runtime policy path, exact mapping policy-vector path list, exact manifest kinds, exact required/covered behavior-family lists, and common migration decision text for mapped values before late errors, late upstream error propagation, cancellation cleanup/suppression, unknown notification filtering, continued known values after unknowns, failed-subscribe observer policy, facade error mapping, known/unknown mapping IDs, raw parameter preservation, typed filesystem/sync/autosync/start-GPS decoding, stop-GPS empty parameters, invalid-payload null parsing, repeated notification ordering, platform vector references, and compile verification; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarD2HNotificationsUtilsTest.d2h stream readiness manifest is pinned before stream runtime migration' --tests 'com.polar.sdk.api.model.utils.PolarD2HNotificationsUtilsTest.d2h mapping readiness manifest is pinned before mapping migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDeviceToHostNotificationsApiTests/testD2HStreamReadinessManifestIsPinnedBeforeStreamRuntimeMigration -only-testing:PolarBleSdkTests/PolarDeviceToHostNotificationsApiTests/testD2HMappingReadinessManifestIsPinnedBeforeMappingMigration -resultBundlePath /tmp/polar-ios-d2h-readiness-exact.xcresult`; Swift parse, Ruby guardrails, and whitespace checks are clean.
- Shared D2H stream runtime readiness now asserts exact stream-runtime policy scenario IDs, expected case IDs, execution kind/transport, wall-clock-safe flag, Android/iOS/common consumer ownership lists, readiness consumer ownership lists, exact common migration decision text, and runtime-prototype metadata instead of policy-term and decision substring checks; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.D2hStreamRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarD2HNotificationsUtilsTest.d2h stream readiness manifest is pinned before stream runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDeviceToHostNotificationsApiTests/testD2HStreamReadinessManifestIsPinnedBeforeStreamRuntimeMigration -resultBundlePath /tmp/polar-ios-d2h-stream-common-exact.xcresult`.
- Shared D2H notification mapping readiness now asserts the exact mapping policy-vector path order, exact required/covered behavior-family lists, exact common migration decision text, and Android/iOS/common consumer ownership metadata instead of path-membership and decision substring checks for known IDs, unknown-ID filtering, raw parameters, typed fields, invalid payloads, repeated ordering, and compile verification; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.D2hNotificationCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarD2HNotificationsUtilsTest.d2h mapping readiness manifest is pinned before mapping migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDeviceToHostNotificationsApiTests/testD2HMappingReadinessManifestIsPinnedBeforeMappingMigration -resultBundlePath /tmp/polar-ios-d2h-mapping-common-exact.xcresult`.
- iOS public D2H notification raw-value mapping now consults `PolarD2hRuntimePlanner.notificationTypeName` before falling back to the existing enum switch, so linked builds share `PolarD2hRuntimePlanning.notificationTypeOrNull` with Android while unlinked builds preserve current raw-value compatibility.
- iOS PMD client measurement-to-public feature mapping now consults `PolarIosSharedBridge.pmdMeasurementTypeName` through `PolarPmdMeasurementRuntimePlanner` before the local fallback switch, matching Android's shared `PolarPmdMeasurementTypeName` lookup while preserving iOS unsupported errors for shared names that have no public `PolarDeviceDataType`.
- iOS PMD control-point response parsing now resolves decoded measurement bytes through shared-backed `PmdMeasurementType.fromId`, so raw and masked measurement IDs follow the same shared `PolarPmdMeasurementTypeName` policy before falling back to the existing enum compatibility path.
- iOS PMD secret production code is now directly pinned through focused XCTest coverage for shared-backed strategy byte lookup, PMD settings serialization, and NONE/XOR decrypt planning via `PmdSecretRuntimePlanner`; AES decrypt remains platform-owned until the documented common AES provider gate is deliberately closed.
- Shared command-runtime policy coverage now asserts exact sync-start/sync-stop platform expectation strings, exact platform-split decision text, and exact reset/H10 command common decision text instead of substring checks in `reset-sync-h10-command-policy.json`; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.CommandRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.command runtime readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- Shared stored-data cleanup policy coverage now asserts exact Android/iOS list-failure strings, exact activity empty-parent path split strings, exact platform-split decision text, and exact cleanup workflow common decision text instead of substring checks in `cleanup-workflow-policy.json`; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.StoredDataCleanupRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.stored data cleanup readiness manifest is pinned before cleanup migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- Firmware utility policy and readiness coverage now assert exact version-comparison, invalid-version, and file-ordering common decision strings, exact policy-vector order, exact required/covered behavior-family order, and Android/iOS/common consumer ownership instead of substring checks, set equality, or broad consumer presence checks in shared common, Android, and iOS firmware utility consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FirmwareUpdateUtilityCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarFirmwareUpdateUtilsTest.firmware utility readiness manifest is pinned before utility migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarFirmwareUpdateUtilsTest/testFirmwareUtilityReadinessManifestIsPinnedBeforeUtilityMigration -resultBundlePath /tmp/polar-ios-firmware-utility-readiness-order-exact.xcresult`.
- Firmware utility readiness now records `compileVerifiedPreMigrationCharacterization` instead of `blockedUntilCompileVerified` after the shared common policy, Android readiness consumer, iOS readiness consumer, and migration policy mirror compile and execute against `utility-readiness.json`; focused validation passes with `ruby scripts/kmp_non_gradle_checks.rb`, the platform readiness audit `TOTAL=0`, `git diff --check`, `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FirmwareUpdateUtilityCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarFirmwareUpdateUtilsTest.firmware utility readiness manifest is pinned before utility migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`, and `xcodebuild test -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme PolarBleSdk -destination 'platform=iOS Simulator,id=0DB7D4B5-705D-4195-8F51-75425EBF2B96,arch=arm64' -only-testing:PolarBleSdkTests/PolarFirmwareUpdateUtilsTest/testFirmwareUtilityReadinessManifestIsPinnedBeforeUtilityMigration`.
- `FirmwareUpdateUtilityCommonPolicyTest.kt` is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so device-info protobuf mapping, zero-version preservation, dotted-integer version comparison, invalid-version typed parse failure, SYSUPDAT.IMG ordering, platform vector references, and compile-verification terms cannot drift out of the shared common policy before firmware utility migration.
- Android and iOS watch-face and KVTX readiness guards now assert exact policy-vector path lists, exact manifest kinds, exact required/covered behavior-family lists, and common migration decision text for watch-face default/scalar fields, complication ordering, empty complication IDs, known/unknown complication lookup, malformed too-short defaulting, KVTX wrapper metadata, KVTX write/commit and empty-data framing, unsigned 32-bit keys, multiple-key selection, append/remove behavior, EX zero-index behavior, non-empty EX index ignore policy, unknown-command stop policy, malformed-script typed error policy, truncated payload platform vectors, platform vector references, and compile verification; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarWatchFaceUtilsTest.watch face readiness manifest is pinned before model migration' --tests 'com.polar.sdk.impl.utils.KvtxScriptUtilsTest.kvtx readiness manifest is pinned before script migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarWatchFaceUtilsTests/testWatchFaceReadinessManifestIsPinnedBeforeModelMigration -only-testing:PolarBleSdkTests/KvtxScriptUtilsTests/testKvtxReadinessManifestIsPinnedBeforeScriptMigration -resultBundlePath /tmp/polar-ios-watchface-kvtx-readiness-exact.xcresult`; Swift parse, Ruby guardrails, and whitespace checks are clean.
- Shared watch-face readiness now asserts exact watch-face policy-vector path order, exact required/covered behavior-family lists, exact common migration decision text, and Android/iOS/common consumer ownership metadata instead of path-membership and decision substring checks for default fields, scalar fields, complication ordering, empty complication IDs, known/unknown complication lookup, malformed defaulting, KVTX wrapper metadata, and compile verification; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.WatchFaceCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarWatchFaceUtilsTest.watch face readiness manifest is pinned before model migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarWatchFaceUtilsTests/testWatchFaceReadinessManifestIsPinnedBeforeModelMigration -resultBundlePath /tmp/polar-ios-watchface-common-exact.xcresult`.
- Android and iOS offline-trigger runtime readiness guards now assert the exact policy-vector path, exact manifest kind, exact required/covered behavior-family list, exact common migration decision text, and executable shared commonTest runtime-prototype status for typed set mode, status read, settings write, optional secret attachment, get/set/status/settings error propagation, enabled/excluded feature projection, platform packet split, facade error mapping deferral, and compile verification; focused Android validation passes from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.utils.PolarDataUtilsTest.trigger runtime readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDataUtilsTest/testTriggerRuntimeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-offline-trigger-readiness-exact.xcresult`; Swift parse, Ruby guardrails, and whitespace checks are clean.
- Shared GNSS location readiness now asserts exact policy-vector path order, exact `sharedParserAndroidProductionDelegation` migration-readiness state, exact required/covered behavior-family order, exact Android/common consumer ownership, and deliberate absence of iOS consumer ownership instead of set equality or missing ownership assertions for raw type 0 coordinate, raw type 1 satellite dilution, raw type 2 satellite summary, raw type 3 NMEA, Android production delegation, non-iOS parser ownership, shared parser parity, and compile verification; focused Android/shared validation should run from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.GnssLocationOwnershipCommonPolicyTest' --tests 'com.polar.sharedtest.SensorParserProductionCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.GnssLocationDataTest' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`; the manifest deliberately remains non-iOS while Android delegates production byte decoding through shared KMP.
- GNSS ownership readiness is also guarded by the cheap Ruby mirror: `scripts/kmp_non_gradle_checks.rb` asserts the executable `GnssLocationOwnershipCommonPolicyTest.kt` still contains raw type 0-3 vector paths, `sharedParserAndroidProductionDelegation`, the deliberate no-iOS consumer assertion, Android/non-iOS/common platform ownership assertions, and `SHARED_GNSS_LOCATION_MIGRATION_OWNERSHIP`; refreshed validation should include `ruby scripts/kmp_non_gradle_checks.rb` and focused Gradle from `sources/Android/android-communications` using `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.GnssLocationOwnershipCommonPolicyTest' --tests 'com.polar.sharedtest.SensorParserProductionCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- iOS PSFTP runtime, generic stream runtime, and fake-transport readiness guards now assert exact `commonDecision` strings instead of substring checks, closing the remaining broad common-decision manifest assertions while preserving exact policy path and behavior-family checks; focused iOS validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/BlePsFtpClientTest/testPsFtpRuntimeReadinessManifestIsPinnedBeforeSharedRuntimeMigration -only-testing:iOSCommunicationsTests/StreamContinuationListTest/testStreamRuntimeReadinessManifestIsPinnedBeforeStreamRuntimeMigration -only-testing:iOSCommunicationsTests/StreamContinuationListTest/testFakeTransportReadinessManifestIsPinnedBeforeSharedRuntimeDelegation -resultBundlePath /tmp/polar-ios-common-decision-readiness-exact.xcresult`; Swift parse, Ruby guardrails, whitespace checks, and the remaining subset/common-decision grep are clean.
- Firmware workflow runtime and readiness guards now assert exact scenario IDs, exact common workflow prototype case IDs, exact migration requirement text, exact common workflow decision text, exact readiness behavior families, exact common readiness decision text, and exact shared runtime-prototype status instead of loose substring checks; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FirmwareWorkflowRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarFirmwareUpdateUtilsTest' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarFirmwareUpdateUtilsTest/testWorkflowRuntimePolicyVectorIsPinnedBeforeWorkflowMigration -only-testing:PolarBleSdkTests/PolarFirmwareUpdateUtilsTest/testWorkflowRuntimeReadinessManifestIsPinnedBeforeWorkflowMigration -only-testing:PolarBleSdkTests/PolarFirmwareUpdateUtilsTest/testFirmwareUtilityReadinessManifestIsPinnedBeforeUtilityMigration -resultBundlePath /tmp/polar-ios-firmware-readiness-exact.xcresult`; `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, and the firmware loose-assertion grep are clean.
- Firmware workflow policy-vector coverage now pins exact Android, iOS, and commonPrototype consumer ownership in the shared common workflow policy test and Android/iOS firmware vector consumers instead of platform key presence checks, so workflow-runtime-policy.json cannot silently drop the Android-hosted common fake workflow or shared common prototype while the readiness manifest remains pinned.
- Firmware workflow readiness coverage now parses `workflow-runtime-policy.json` from the shared common readiness test and asserts exact vector id, vector case, workflow kind, scenario order, common workflow prototype case order, fake-network/filesystem/BLE migration requirement, workflow policy decision, execution metadata, scenario-specific update availability, download failure, retryable server failure, invalid zip, cancellation cleanup before BLE writes, system-update write-last ordering, reboot success, battery-too-low terminal error, expected writes/statuses, and notes text instead of raw policy string containment when `workflow-runtime-readiness.json` references the policy vector; the retired Android-hosted `FirmwareUpdateCommonFakeWorkflowTest.kt` prototype has been removed, and focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FirmwareWorkflowRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarFirmwareUpdateUtilsTest.workflow runtime policy vector is pinned before workflow migration' --tests 'com.polar.sdk.api.model.utils.PolarFirmwareUpdateUtilsTest.workflow runtime readiness manifest is pinned before workflow migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarFirmwareUpdateUtilsTest/testWorkflowRuntimePolicyVectorIsPinnedBeforeWorkflowMigration -only-testing:PolarBleSdkTests/PolarFirmwareUpdateUtilsTest/testWorkflowRuntimeReadinessManifestIsPinnedBeforeWorkflowMigration -resultBundlePath /tmp/polar-ios-firmware-workflow-policy-structured.xcresult`.
- Shared offline-trigger runtime coverage now asserts exact scenario IDs, exact common runtime prototype case IDs, exact policy kind, exact policy string, exact runtime common decision, exact shared execution status, exact platform-expectation common text, exact Android/iOS cleanup evidence IDs, exact readiness behavior families, exact readiness common decision, and exact runtime-prototype status instead of substring checks while Android and iOS platform trigger readiness consumers continue to pin the same vector; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.OfflineTriggerRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.utils.PolarDataUtilsTest.trigger runtime policy vector is pinned before runtime migration' --tests 'com.polar.sdk.impl.utils.PolarDataUtilsTest.trigger runtime readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDataUtilsTest/testTriggerRuntimePolicyVectorIsPinnedBeforeRuntimeMigration -only-testing:PolarBleSdkTests/PolarDataUtilsTest/testTriggerRuntimeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-offline-trigger-common-exact.xcresult`; `ruby scripts/kmp_non_gradle_checks.rb` and `git diff --check` are clean.
- Offline-trigger runtime policy-vector coverage now pins exact Android, iOS, and commonPrototype consumer ownership in the shared common policy test and Android/iOS trigger vector consumers instead of platform key presence checks, so trigger-runtime-policy.json cannot silently drop PMD adapter, public facade, platform utility, Android-hosted fake runtime, or shared common prototype consumers while the readiness manifest remains pinned.
- Offline-trigger runtime policy-vector coverage now parses `trigger-runtime-policy.json` in shared common, Android facade, Android PMD adapter, iOS facade, and iOS PMD adapter consumers and asserts exact vector id, vector case, runtime kind, scenario ID order, common runtime prototype case ID order, trigger mode, secret presence, set-mode/status/settings transport failures, enabled/excluded feature projection, cleanup evidence IDs, platform expectation text, execution status, common decision text, and the explicit length-byte packet split note instead of raw JSON string containment when platform tests reference trigger runtime or cleanup-evidence cases; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.OfflineTriggerRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.setOfflineRecordingTrigger maps public trigger and secret to PMD facade request' --tests 'com.polar.sdk.impl.BDBleApiImplTest.setOfflineRecordingTrigger propagates PMD facade error with mapped payload prepared' --tests 'com.polar.sdk.impl.BDBleApiImplTest.getOfflineRecordingTriggerSetup maps PMD status to public trigger and propagates errors' --tests 'com.polar.sdk.impl.BDBleApiImplTest.offline trigger runtime readiness manifest is pinned before runtime migration' --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.BlePmdClientTest.setOfflineRecordingTrigger sends Android length-prefixed setting and secret packets' --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.BlePmdClientTest.getOfflineRecordingTriggerStatus ignores stale response for different command' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_setOfflineRecordingTrigger_mapsPublicTriggerAndSecretToPmdControlPointSequence -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_setOfflineRecordingTrigger_setModeErrorPropagatesAndStopsBeforeStatusRead -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_getOfflineRecordingTriggerSetup_mapsPmdStatusToPublicTriggerAndPropagatesErrors -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testOfflineTriggerRuntimeReadinessManifestIsPinnedBeforeRuntimeMigration -only-testing:iOSCommunicationsTests/BlePmdClientTest/testSetOfflineRecordingTriggerSendsIOSSettingAndSecretPacketsWithoutLengthPrefix -only-testing:iOSCommunicationsTests/BlePmdClientTest/testGetOfflineRecordingTriggerStatusClearsStaleControlPointResponsesBeforeTransmit -resultBundlePath /tmp/polar-ios-offline-trigger-policy-structured.xcresult`.
- REST request transport runtime coverage now asserts exact request scenario IDs, exact common runtime prototype case IDs, exact migration requirement text, exact common empty-response decision, exact shared execution status, exact platform-expectation common text, exact required/covered readiness behavior-family order, exact readiness common decision, exact runtime-prototype status/reason, and Android/iOS/common consumer ownership instead of sorted equality or substring checks in the shared common gate and the Android/iOS vector consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.RestRequestTransportPolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarDeviceRestApiUtilsTest.restRequestTransportReadinessManifest_isPinnedBeforeRuntimeMigration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDeviceRestApiServiceTests/testRestRequestTransportReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-rest-request-transport-readiness-order-exact.xcresult`; `ruby scripts/kmp_non_gradle_checks.rb` and `git diff --check` are clean.
- REST request transport runtime execution now runs `rest-request-transport-policy.json` through production shared `PolarRuntimeOrchestration.planRestRequestTransport` for deterministic GET path capture, response-error status/message planning, and empty successful response policy while public Android/iOS facade error mapping and BLE/PFTP transport calls remain adapter-owned; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.RestRequestTransportPolicyCommonTest' --tests 'com.polar.sharedtest.RestFacadeRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all`, `ruby scripts/kmp_non_gradle_checks.rb`, and `git diff --check`.
- Android REST request transport vector consumers now also pin the exact request scenario IDs and common runtime prototype case IDs for `service-list-request-error-payload`, `service-description-request-error-payload`, `service-list-empty-transport-response`, and `service-description-empty-transport-response` in `PolarDeviceRestApiUtilsTest.kt` and the shared `RestRequestTransportPolicyCommonTest.kt` instead of only referencing `rest-request-transport-policy.json`; the retired Android-hosted `RestAndFileCommonFakeRuntimeTest.kt` prototype has been removed, and validation passes with `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, and from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.RestRequestTransportPolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarDeviceRestApiUtilsTest.restRequestTransportPolicyVector_isPinnedBeforeRuntimeMigration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- REST request transport policy-vector coverage now pins exact Android, iOS, and commonPrototype consumer ownership in the shared common policy test and Android/iOS REST service vector consumers instead of platform key presence checks, so rest-request-transport-policy.json cannot silently drop the Android-hosted common fake runtime or shared common prototype while the readiness manifest remains pinned.
- REST request transport readiness coverage now parses `rest-request-transport-policy.json` from the shared common readiness test and asserts exact vector id, vector case, runtime kind, request scenario order, common runtime prototype case order, service-list and service-description GET paths, PFTP response-error modes/status/messages, empty successful payload gates, execution status, platform expectation text, notes, migration requirement, and common decision text instead of raw policy string containment when `rest-request-transport-readiness.json` references the policy vector; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.RestRequestTransportPolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarDeviceRestApiUtilsTest.restRequestTransportPolicyVector_isPinnedBeforeRuntimeMigration' --tests 'com.polar.sdk.api.model.utils.PolarDeviceRestApiUtilsTest.restRequestTransportReadinessManifest_isPinnedBeforeRuntimeMigration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDeviceRestApiServiceTests/testRestRequestTransportPolicyVectorIsPinnedBeforeRuntimeMigration -only-testing:PolarBleSdkTests/PolarDeviceRestApiServiceTests/testRestRequestTransportReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-rest-request-transport-policy-structured.xcresult`.
- REST event compression coverage now asserts exact compression policy case IDs, exact uncompressed and empty-batch behavior, exact Android gzip and iOS deflate codec split, exact malformed compressed payload platform outcomes, exact malformed payload policy strings, exact common codec ownership decision text, exact required/covered readiness behavior-family order, exact readiness common decision, and Android/iOS/common consumer ownership instead of sorted equality or broad substring checks in the shared common gate and Android/iOS REST event consumers; Android and iOS production REST event compressed payload decoding now delegates through shared KMP platform actual codecs while platform adapters keep protobuf parsing, notification streams, logging, and public error behavior.
- REST facade runtime coverage now asserts exact public facade operation IDs, exact common runtime prototype case IDs, exact shared runtime decision text, exact top-level migration decision text, exact fake-runtime execution kind/transport, exact readiness behavior families, exact readiness common decision, and exact runtime-prototype status instead of substring checks in the shared common gate; Android and iOS facade readiness helpers now support exact `commonDecision` assertions and use them for `rest-facade-runtime-readiness.json`; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.RestFacadeRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.rest facade readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --no-daemon --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testRestFacadeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-rest-facade-readiness-exact.xcresult`; `ruby scripts/kmp_non_gradle_checks.rb` and `git diff --check` are clean.
- File runtime error coverage now asserts exact runtime-error scenario IDs, exact common runtime prototype case IDs, exact migration requirement text, exact typed-error common decision, exact shared execution status, exact platform-expectation common text, exact required/covered readiness behavior-family order, exact readiness common decision, exact runtime-prototype status/reason, and Android/iOS/common consumer ownership instead of sorted equality or substring checks in the shared common gate and the Android/iOS file utility vector consumers; the file read/write/delete vector now runs deterministic GET/PUT/REMOVE command, payload, and result planning through production shared `PolarRuntimeOrchestration.planFileFacade` while public error mapping remains adapter-owned. Focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FileRuntimeErrorPolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarFileUtilsTest.runtimeErrorReadinessManifest_isPinnedBeforeRuntimeMigration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarFileUtilsTest/testRuntimeErrorReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-file-runtime-error-readiness-order-exact.xcresult`; `ruby scripts/kmp_non_gradle_checks.rb` and `git diff --check` are clean.
- File runtime-error readiness coverage now parses `runtime-error-policy.json` from the shared common readiness test and asserts exact vector id, vector case, runtime kind, runtime-error case order, common runtime prototype case order, list/read/write/delete operation names, command paths, malformed directory payload, response-error statuses/messages, transport/write-stream errors, write payload capture, execution status, platform expectation text, notes, migration requirement, and common decision text instead of raw policy string containment when `runtime-error-readiness.json` references the policy vector; the retired Android-hosted `RestAndFileCommonFakeRuntimeTest.kt` prototype has been removed, and focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FileRuntimeErrorPolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarFileUtilsTest.runtimeErrorPolicyVector_isPinnedBeforeRuntimeMigration' --tests 'com.polar.sdk.api.model.utils.PolarFileUtilsTest.runtimeErrorReadinessManifest_isPinnedBeforeRuntimeMigration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarFileUtilsTest/testRuntimeErrorPolicyVectorIsPinnedBeforeRuntimeMigration -only-testing:PolarBleSdkTests/PolarFileUtilsTest/testRuntimeErrorReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-file-runtime-error-policy-structured.xcresult`.
- File facade runtime coverage now asserts exact low-level operation IDs, exact common runtime prototype case IDs, exact command paths, exact read response bytes, exact write payload bytes, exact progress markers, exact platform response-error terminal markers, exact shared runtime decision text, exact top-level migration decision text, exact execution kind/transport, exact linked file/list/runtime policy notes, exact required/covered readiness behavior-family order, exact readiness common decision, exact runtime-prototype status/reason, and Android/iOS/common consumer ownership instead of sorted equality or broad substring checks in the shared common gate and Android/iOS facade readiness consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FileFacadeRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.file facade readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testFileFacadeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-file-facade-readiness-order-exact.xcresult`.
- File runtime-error and low-level file-facade policy-vector coverage now pins exact Android, iOS, and commonPrototype consumer ownership in the shared common policy tests and Android/iOS `PolarFileUtilsTest` policy-vector consumers instead of platform key presence checks, so runtime-error-policy.json and file-facade-runtime-policy.json cannot silently drop cross-platform consumers while the readiness manifests remain pinned.
- File facade runtime policy-vector coverage now parses `file-facade-runtime-policy.json` in Android and iOS facade consumers and asserts exact vector id, vector case, runtime kind, low-level operation ID order, common runtime prototype case ID order, representative read/write/delete commands, paths, read response bytes, write payload bytes, write progress values, stream-failure mode, response-error platform terminals, execution kind/transport, and common decision text instead of raw JSON string containment when platform tests reference file facade runtime cases; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FileFacadeRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.readFile reads low level file path from fake transport' --tests 'com.polar.sdk.impl.BDBleApiImplTest.readFile preserves empty low level file payload as success' --tests 'com.polar.sdk.impl.BDBleApiImplTest.readFile propagates low level request failure' --tests 'com.polar.sdk.impl.BDBleApiImplTest.readFile maps low level pftp response error to enum name' --tests 'com.polar.sdk.impl.BDBleApiImplTest.writeFile writes low level file payload' --tests 'com.polar.sdk.impl.BDBleApiImplTest.writeFile consumes low level write progress before success' --tests 'com.polar.sdk.impl.BDBleApiImplTest.writeFile propagates low level write failure after payload is prepared' --tests 'com.polar.sdk.impl.BDBleApiImplTest.writeFile propagates low level pftp response error after payload is prepared' --tests 'com.polar.sdk.impl.BDBleApiImplTest.deleteFileOrDirectory sends low level remove request' --tests 'com.polar.sdk.impl.BDBleApiImplTest.deleteFileOrDirectory propagates low level request failure' --tests 'com.polar.sdk.impl.BDBleApiImplTest.deleteFileOrDirectory maps low level pftp response error to enum name' --tests 'com.polar.sdk.impl.BDBleApiImplTest.file facade readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_readFile_readsLowLevelFilePathFromFakeTransport -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_readFile_emptyLowLevelFilePayloadSucceeds -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_readFile_requestError_propagatesError -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_readFile_responseError_wrapsAsDeviceErrorWithPftpDetails -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_writeFile_writesLowLevelFilePayload -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_writeFile_consumesLowLevelWriteProgressBeforeSuccess -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_writeFile_writeError_propagatesErrorAfterPayloadIsPrepared -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_writeFile_responseError_preservesPftpErrorAfterPayloadIsPrepared -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_deleteFileOrDirectory_sendsLowLevelRemoveRequest -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_deleteFileOrDirectory_requestError_propagatesError -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_deleteFileOrDirectory_responseError_wrapsAsDeviceErrorWithPftpDetails -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testFileFacadeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-file-facade-policy-structured.xcresult`.
- File facade runtime readiness coverage now also pins the exact behavior-family order in Android and iOS `PolarFileUtilsTest` consumers, including low-level path gates, read/write/delete success and error families, directory-list vector reference gates, runtime-error policy references, malformed-directory and response-error gates, facade-error mapping, platform facade references, and compile verification; focused validation passes with the readiness-family audit shrinking to the remaining non-file gaps, `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, `swiftc -parse sources/iOS/ios-communications/Tests/PolarBleSdkTests/PolarFileUtilsTest.swift`, Android `PolarFileUtilsTest.fileFacadeRuntimeReadinessManifest_isPinnedBeforeRuntimeMigration` plus `GoldenVectorMigrationPolicyTest`, and iOS `PolarFileUtilsTest/testFileFacadeRuntimeReadinessManifestIsPinnedBeforeRuntimeMigration`.
- Backup workflow readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, exact common decision text, and Android/iOS/common consumer ownership instead of sorted equality or presence-only checks in the shared common gate and Android/iOS backup vector consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.BackupUtilityCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarBackupManagerTest.backup workflow readiness manifest is pinned before workflow migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBackupManagerTest/testBackupWorkflowReadinessManifestIsPinnedBeforeWorkflowMigration -resultBundlePath /tmp/polar-ios-backup-workflow-readiness-order-exact.xcresult`.
- Backup restore-failure policy coverage now asserts exact restore policy vector id/case, PUT command order, failed path, Android/iOS platform split, consumer ownership, platform flags, notes, and iOS aggregate localized-description shape instead of substring matching in the shared common backup policy test and iOS backup vector consumer; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.BackupUtilityCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarBackupManagerTest.backup golden vectors preserve Android restore failure policy' --tests 'com.polar.sdk.api.model.utils.PolarBackupManagerTest.backup workflow readiness manifest is pinned before workflow migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`, `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBackupManagerTest/testBackupGoldenVectorsPreserveIOSRestoreFailurePolicy -only-testing:PolarBleSdkTests/PolarBackupManagerTest/testBackupWorkflowReadinessManifestIsPinnedBeforeWorkflowMigration -resultBundlePath /tmp/polar-ios-backup-restore-failure-structured-3.xcresult`, and `ruby scripts/kmp_non_gradle_checks.rb`.
- Command runtime readiness coverage now asserts exact required/covered behavior-family order, exact common decision text, exact runtime-prototype status/reason, and Android/iOS/common consumer ownership instead of sorted equality, broad decision substring checks, or consumer presence-only checks in the shared common gate and Android/iOS command facade consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.CommandRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.command runtime readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testCommandRuntimeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-command-runtime-readiness-order-exact.xcresult`.
- Command runtime policy-vector coverage now parses reset-sync-h10-command-policy.json in shared common, Android, and iOS facade consumers and asserts exact vector id, vector case, operation ID order, common runtime prototype case ID order, execution kind/transport, H10 query IDs/error labels, start-recording parameters, and common decision text instead of raw JSON string containment when platform tests reference command runtime cases.
- Command runtime policy mirrors now require every reset/sync/H10 runtime scenario ID consumed by shared and platform tests, including `factory-reset-notification-failure`, `sync-start-success`, and `sync-stop-success`; the runtime scenario mirror audit now passes across shared common tests, Gradle policy guards, and the Ruby mirror.
- Stored-data cleanup readiness coverage now asserts exact required/covered behavior-family order, exact common decision text, exact runtime-prototype status/reason, and Android/iOS/common consumer ownership instead of sorted equality, broad decision substring checks, or consumer presence-only checks in the shared common gate and Android/iOS cleanup facade consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.StoredDataCleanupRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.stored data cleanup readiness manifest is pinned before cleanup migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testStoredDataCleanupReadinessManifestIsPinnedBeforeCleanupMigration -resultBundlePath /tmp/polar-ios-stored-data-cleanup-readiness-order-exact.xcresult`.
- Stored-data cleanup policy-vector coverage now parses cleanup-workflow-policy.json in shared common, Android, and iOS facade consumers and asserts exact vector id, vector case, scenario ID order, common runtime prototype case ID order, execution kind/transport, representative filter/prune inputs, platform split expectations, and common decision text instead of raw JSON string containment when platform tests reference cleanup runtime cases.
- Stored-data cleanup runtime coverage now includes the previously implicit telemetry root list-failure platform split as `telemetry-list-failure-platform-policy`, with Android/iOS facade tests referencing the same vector and shared common runtime policy asserting exact scenario order and `GET:/` command capture; focused validation passes with `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.StoredDataCleanupRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.deleteTelemetryData swallows list failure' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`, and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,id=0DB7D4B5-705D-4195-8F51-75425EBF2B96,arch=arm64' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_deleteTelemetryData_listFailurePropagatesError -resultBundlePath /tmp/polar-ios-telemetry-cleanup-list-failure.xcresult`.
- Firmware workflow readiness coverage now asserts exact required/covered behavior-family order, exact common decision text, exact runtime-prototype status/reason, and Android/iOS/common consumer ownership instead of sorted equality, broad decision substring checks, or consumer presence-only checks in the shared common gate and Android/iOS firmware workflow consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FirmwareWorkflowRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarFirmwareUpdateUtilsTest.workflow runtime readiness manifest is pinned before workflow migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarFirmwareUpdateUtilsTest/testWorkflowRuntimeReadinessManifestIsPinnedBeforeWorkflowMigration -resultBundlePath /tmp/polar-ios-firmware-workflow-readiness-order-exact.xcresult`.
- Firmware workflow policy mirrors now require every workflow-runtime scenario ID already consumed by shared, Android, and iOS tests, including `check-update-not-available`, `check-update-available`, `download-failure`, `empty-or-invalid-zip`, `write-package-success-with-system-update-last`, `system-update-reboot-response-is-success`, and `battery-too-low-response-is-terminal-failure`; focused validation passes with `ruby scripts/kmp_non_gradle_checks.rb`, a mechanical shared/Android/iOS/Gradle/Ruby scenario-reference audit, `git diff --check`, and `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.FirmwareWorkflowRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarFirmwareUpdateUtilsTest.workflow runtime readiness manifest is pinned before workflow migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- Offline trigger runtime readiness coverage now asserts exact required/covered behavior-family order, exact common decision text, exact runtime-prototype status/reason, and Android/iOS/common consumer ownership instead of sorted equality, subset checks, or consumer presence-only checks in the shared common gate and Android/iOS trigger runtime consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.OfflineTriggerRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.utils.PolarDataUtilsTest.trigger runtime readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDataUtilsTest/testTriggerRuntimeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-offline-trigger-readiness-order-exact.xcresult`.
- Offline-trigger runtime policy mirrors now require every trigger-runtime scenario ID consumed by shared, Android, and iOS tests, including `set-trigger-success-with-secret`, `set-trigger-mode-error`, `set-trigger-status-read-error`, `set-trigger-setting-error`, `get-trigger-success`, and `get-trigger-transport-error`; focused validation passes with the runtime scenario mirror audit and `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.OfflineTriggerRuntimePolicyCommonTest' --tests 'com.polar.sharedtest.CommandRuntimePolicyCommonTest' --tests 'com.polar.sharedtest.FirmwareWorkflowRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- Platform runtime policy-vector consumers now have no weak scenario/case references in the repository-wide mirror audit; file-runtime consumers pin `directory-list-response-error-103`, `directory-list-malformed-payload`, `read-file-transport-error`, `write-file-stream-error-after-header`, and `delete-file-response-error`, while offline-trigger consumers pin `set-trigger-success-with-secret`, `set-trigger-mode-error`, `set-trigger-status-read-error`, `set-trigger-setting-error`, `get-trigger-success`, and `get-trigger-transport-error`; focused validation passes with the scenario-reference audit returning `TOTAL=0`, `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, Android Gradle policy tests for the touched consumers, and focused iOS XCTest for `PolarDataUtilsTest/testTriggerRuntimePolicyVectorIsPinnedBeforeRuntimeMigration`.
- Runtime scenario policy mirrors now cover every shared common runtime vector case currently consumed through `consumerTests.commonPrototype`, including D2H stream late-error/cancellation/filtering/failed-subscribe cases, REST request transport request-error and empty-response cases, file runtime directory/read/write/delete error cases, and user-device-settings device-location write failure; validation passes with the common runtime vector-id mirror audit, `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, and `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.D2hStreamRuntimePolicyCommonTest' --tests 'com.polar.sharedtest.FileRuntimeErrorPolicyCommonTest' --tests 'com.polar.sharedtest.RestRequestTransportPolicyCommonTest' --tests 'com.polar.sharedtest.UserDeviceSettingsRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- REST event compression and training-session payload-parser mirrors now require the exact policy case IDs already asserted by shared, Android, and iOS tests, including `uncompressed-batch`, `empty-uncompressed-batch`, and every protobuf/gzip training payload parser family; validation passes with the REST/training case mirror audit, `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, and `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.RestEventCompressionPolicyCommonTest' --tests 'com.polar.sharedtest.TrainingSessionCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- iOS runtime policy-vector consumers now assert exact shared-runtime scenario IDs for D2H stream runtime, REST request transport, and file runtime-error vectors instead of only referencing the owning vector/readiness manifest; validation passes with the platform/common scenario-reference audit, `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,id=0DB7D4B5-705D-4195-8F51-75425EBF2B96,arch=arm64' -only-testing:PolarBleSdkTests/PolarDeviceToHostNotificationsApiTests/testD2HStreamReadinessManifestIsPinnedBeforeStreamRuntimeMigration -only-testing:PolarBleSdkTests/PolarDeviceRestApiServiceTests/testRestRequestTransportPolicyVectorIsPinnedBeforeRuntimeMigration -only-testing:PolarBleSdkTests/PolarFileUtilsTest/testRuntimeErrorPolicyVectorIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-runtime-scenario-reference-pins.xcresult`.
- D2H stream runtime readiness coverage now asserts exact required/covered behavior-family order, exact common decision text, exact runtime-prototype status/reason, and Android/iOS/common consumer ownership instead of sorted equality or missing platform prototype/consumer assertions in the shared common gate and Android/iOS D2H stream consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.D2hStreamRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarD2HNotificationsUtilsTest.d2h stream readiness manifest is pinned before stream runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDeviceToHostNotificationsApiTests/testD2HStreamReadinessManifestIsPinnedBeforeStreamRuntimeMigration -resultBundlePath /tmp/polar-ios-d2h-stream-readiness-order-exact.xcresult`.
- Android D2H stream runtime vector coverage now parses `stream-runtime-policy.json` from the platform readiness guard and asserts exact input scenario ID order plus expected case ID order, including `failed-subscribe-does-not-register-observer`, so the Android platform consumer no longer relies on individual behavior tests to mention only the late-error, cancellation, and unknown-filtering scenarios; validation passes with the targeted D2H Android scenario-reference audit, `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, and from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarD2HNotificationsUtilsTest.d2h stream readiness manifest is pinned before stream runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- D2H notification mapping readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, exact common decision text, and Android/iOS/common consumer ownership instead of sorted equality or missing platform consumer assertions in the shared common gate and Android/iOS D2H mapping consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.D2hNotificationCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarD2HNotificationsUtilsTest.d2h mapping readiness manifest is pinned before mapping migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDeviceToHostNotificationsApiTests/testD2HMappingReadinessManifestIsPinnedBeforeMappingMigration -resultBundlePath /tmp/polar-ios-d2h-mapping-readiness-order-exact.xcresult`.
- Gradle warning cleanup and iOS XCTest unblock are validated before the next readiness slice: `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew help --warning-mode all`, `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --warning-mode all`, focused shared/library migration tests, and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme PolarBleSdk -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/GoldenVectorTestDataTest/testGoldenVectorLoaderFindsRepositoryRootFromEnvironment -only-testing:iOSCommunicationsTests/GoldenVectorTestDataTest/testGoldenVectorLoaderFindsRepositoryRootFromEnvironment -resultBundlePath /tmp/polar-ios-polarblesdk-scheme-unblock.xcresult` now pass without reproduced Gradle warnings or missing XCTest testables.
- Disk/time runtime readiness coverage now asserts exact required/covered behavior-family order, exact common decision text, exact runtime-prototype status/reason, and Android/iOS/common consumer ownership instead of sorted equality or broad decision substring checks in the shared common gate and Android/iOS facade consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.DiskTimeRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.disk time readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testDiskTimeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-disk-time-readiness-order-exact.xcresult`.
- Android activity, sleep, nightly recharge, skin-temperature, SpO2, calories, distance, active-time facade date loops, and automatic-sample embedded-day filters now route inclusive `yyyyMMdd` sequencing through shared `PolarTimeUtils.basicDateRange`; Android still owns `LocalDate` conversion, PSFTP reads, REST/file paths, protobuf parsing, public models, and public error mapping. iOS steps, distance, nightly recharge, calories, activity, daily-summary, active-time, SpO2, skin-temperature, sleep, date-folder deletion loops, and automatic-sample embedded-day filters now consume the same shared `yyyyMMdd` range through `PolarIosSharedBridge.basicDateRangeCsv`, while Swift keeps `Date`/`Calendar` conversion, end-time clipping, filesystem reads, protobuf parsing, public models, and public error mapping platform-owned.
- Disk/time runtime policy-vector coverage now parses disk-time-query-policy.json in shared common, Android, and iOS facade consumers and asserts exact vector id, vector case, operation ID order, common runtime prototype case ID order, execution kind/transport, query IDs, V2/H10 time-field expectations, and common decision text instead of raw JSON string containment when platform tests reference disk/time runtime cases.
- User-device-settings runtime readiness coverage now asserts exact required/covered behavior-family order, exact common decision text, exact runtime-prototype status/reason, and Android/iOS/common consumer ownership instead of sorted equality or broad decision substring checks in the shared common gate and Android/iOS facade consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.UserDeviceSettingsRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.user device settings readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testUserDeviceSettingsReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-user-device-settings-readiness-order-exact.xcresult`.
- User-device-settings runtime policy-vector coverage now parses settings-runtime-policy.json in shared common, Android, and iOS facade consumers and asserts exact vector id, vector case, settings path, operation ID order, common runtime prototype case ID order, execution kind/transport, key payload-field groups, and common decision text instead of raw JSON string containment when platform tests reference settings runtime cases.
- REST facade runtime readiness coverage now asserts exact required/covered behavior-family order, exact common decision text, exact runtime-prototype status/reason, and Android/iOS/common consumer ownership instead of sorted equality, status-only prototype checks, or missing platform consumer assertions in the shared common gate and Android/iOS facade consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.RestFacadeRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.rest facade readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testRestFacadeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-rest-facade-readiness-order-exact.xcresult`.
- REST facade runtime policy-vector coverage now parses `rest-facade-runtime-policy.json` in shared common, Android, and iOS facade consumers and asserts exact vector id, vector case, runtime kind, public facade operation ID order, common runtime prototype case ID order, representative request paths, payload shapes, response-error terminals, empty/malformed success modes, execution kind/transport, and common decision text instead of raw JSON string containment when platform tests reference REST facade runtime cases; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.RestFacadeRuntimePolicyCommonTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.BDBleApiImplTest.listRestApiServices returns decoded services from fake transport' --tests 'com.polar.sdk.impl.BDBleApiImplTest.getRestApiDescription returns decoded description from fake transport' --tests 'com.polar.sdk.impl.BDBleApiImplTest.getRestApiDescription maps request failure to public error' --tests 'com.polar.sdk.impl.BDBleApiImplTest.getRestApiDescription maps response error to public error' --tests 'com.polar.sdk.impl.BDBleApiImplTest.getRestApiDescription maps empty success payload to parse failure' --tests 'com.polar.sdk.impl.BDBleApiImplTest.getRestApiDescription maps malformed success payload to parse failure' --tests 'com.polar.sdk.impl.BDBleApiImplTest.listRestApiServices maps request failure to public error' --tests 'com.polar.sdk.impl.BDBleApiImplTest.listRestApiServices maps response error to public error' --tests 'com.polar.sdk.impl.BDBleApiImplTest.listRestApiServices maps empty success payload to parse failure' --tests 'com.polar.sdk.impl.BDBleApiImplTest.listRestApiServices maps malformed success payload to parse failure' --tests 'com.polar.sdk.impl.BDBleApiImplTest.rest facade readiness manifest is pinned before runtime migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_listRestApiServices_returnsDecodedServicesFromFakeTransport -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_getRestApiDescription_returnsDecodedDescriptionFromFakeTransport -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_getRestApiDescription_mapsRequestFailureToPublicError -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_getRestApiDescription_mapsResponseErrorToPublicError -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_getRestApiDescription_mapsEmptySuccessPayloadToDecodeFailure -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_getRestApiDescription_mapsMalformedSuccessPayloadToDecodeFailure -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_listRestApiServices_mapsRequestFailureToPublicError -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_listRestApiServices_mapsResponseErrorToPublicError -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_listRestApiServices_mapsEmptySuccessPayloadToDecodeFailure -only-testing:PolarBleSdkTests/PolarBleApiImplTests/test_listRestApiServices_mapsMalformedSuccessPayloadToDecodeFailure -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testRestFacadeReadinessManifestIsPinnedBeforeRuntimeMigration -resultBundlePath /tmp/polar-ios-rest-facade-policy-structured.xcresult`.
- KVTX readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, exact common decision text, and Android/iOS/common consumer ownership instead of sorted equality, subset checks, or broad decision substring checks in the shared common gate and Android/iOS KVTX consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.KvtxCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.impl.utils.KvtxScriptUtilsTest.kvtx readiness manifest is pinned before script migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/KvtxScriptUtilsTests/testKvtxReadinessManifestIsPinnedBeforeScriptMigration -resultBundlePath /tmp/polar-ios-kvtx-readiness-order-exact.xcresult`.
- KVTX byte-level script framing and extraction now live in shared production `PolarKvtxScriptCodec`; Android `KvtxScriptUtils` delegates directly to the shared codec, iOS `KvtxScriptUtils` delegates through `PolarIosSharedBridge` when `PolarBleSdkShared` is linked and keeps the Swift fallback for SwiftPM/watchOS compatibility, and `KvtxCommonPolicyTest.kt` executes the shared production codec instead of a local prototype.
- Watch-face readiness coverage now asserts exact required/covered behavior-family order and Android/iOS/common consumer ownership instead of sorted family equality or missing platform consumer assertions in the shared common gate and Android/iOS watch-face consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.WatchFaceCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.PolarWatchFaceUtilsTest.watch face readiness manifest is pinned before model migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarWatchFaceUtilsTests/testWatchFaceReadinessManifestIsPinnedBeforeModelMigration -resultBundlePath /tmp/polar-ios-watch-face-readiness-order-exact.xcresult`.
- PPG parser readiness coverage now asserts exact required vector path order, exact required/covered frame-family order, exact common decision text, and Android/iOS/common consumer ownership instead of sorted equality, set equality, or broad decision substring checks in the shared common gate and Android/iOS PPG consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PpgParserCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.PpgDataTest.ppgFrameFamilyReadinessManifestPinsAndroidMigrationGate' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/PpgDataTest/testPpgFrameFamilyReadinessManifestPinsIOSMigrationGate -resultBundlePath /tmp/polar-ios-ppg-readiness-order-exact.xcresult`.
- PMD control-point readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, exact common decision text, and Android/iOS/common consumer ownership instead of sorted equality, set equality, or broad substring checks in the shared common gate, Android control-point and active-measurement consumers, and iOS control-point and active-measurement consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PmdControlPointCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.BlePmdClientControlPointResponseTest.PMD control point readiness manifest is pinned before response parser migration' --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.PmdActiveMeasurementTest.PMD control point readiness manifest pins active measurement coverage before migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/BlePmdClientTest/testPmdControlPointReadinessManifestIsPinnedBeforeResponseParserMigration -only-testing:iOSCommunicationsTests/PmdActiveMeasurementTest/testPmdControlPointReadinessManifestPinsActiveMeasurementCoverageBeforeMigration -resultBundlePath /tmp/polar-ios-pmd-control-point-readiness-order-exact.xcresult`.
- Device ID readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, exact common decision text, and Android/iOS/common consumer ownership instead of set equality or broad substring checks in the shared common gate and Android/iOS device ID consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.DeviceIdCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.polar.DeviceIdGoldenVectorTest.device ID readiness manifest is pinned before checksum and UUID migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/BlePolarDeviceIdUtilityTest/testDeviceIdReadinessManifestIsPinnedBeforeChecksumMigration -resultBundlePath /tmp/polar-ios-device-id-readiness-order-exact.xcresult`.
- SDK-level iOS device UUID readiness coverage now matches the exact device-ID readiness contract as well, asserting policy-vector order, required/covered behavior-family order, common decision text, and Android/iOS/common consumer ownership in `PolarDeviceUuidTest` instead of set equality; focused validation passes with `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarDeviceUuidTest/testDeviceIdReadinessManifestIsPinnedBeforeUuidMigration -resultBundlePath /tmp/polar-ios-device-uuid-readiness-order-exact.xcresult` and `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- SDK-level iOS service-client device-ID readiness coverage now matches the exact checksum/UUID/identifier-routing readiness contract in `PolarServiceClientUtilsTest`, so the final declared iOS consumer pins policy-vector order, required/covered behavior-family order, common decision text, and Android/iOS/common consumer ownership instead of relying on identifier-routing vectors alone; focused validation passes with the readiness-family audit removing the device-ID row, `swiftc -parse sources/iOS/ios-communications/Tests/PolarBleSdkTests/PolarServiceClientUtilsTest.swift`, `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarServiceClientUtilsTest/testDeviceIdReadinessManifestIsPinnedBeforeIdentifierRoutingMigration -resultBundlePath /tmp/polar-ios-device-id-service-readiness-family-pins.xcresult`, `ruby scripts/kmp_non_gradle_checks.rb`, and `git diff --check`.
- ACC parser readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, and Android/iOS/common consumer ownership instead of set equality or broad path containment checks in the shared common gate and Android/iOS ACC consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.AccParserCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.AccDataTest.ACC readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/AccDataTest/testAccReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-acc-readiness-order-exact.xcresult`.
- GYR parser readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, and Android/iOS/common consumer ownership instead of set equality or broad path containment checks in the shared common gate and Android/iOS GYR consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.GyrParserCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.GyrDataTest.GYR readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/GyrDataTest/testGyrReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-gyr-readiness-order-exact.xcresult`.
- MAG parser readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, and Android/iOS/common consumer ownership instead of set equality or broad path containment checks in the shared common gate and Android/iOS MAG consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.MagParserCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.MagDataTest.MAG readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/MagDataTest/testMagReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-mag-readiness-order-exact.xcresult`.
- Offline HR parser readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, and Android/iOS/common consumer ownership instead of set equality or broad path containment checks in the shared common gate and Android/iOS Offline HR consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.OfflineHrParserCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.OfflineHrDataTest.offline HR readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/OfflineHrDataTest/testOfflineHrReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-offline-hr-readiness-order-exact.xcresult`.
- ECG parser readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, and Android/iOS/common consumer ownership instead of set equality or broad path containment checks in the shared common gate and Android/iOS ECG consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.EcgParserCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.EcgDataTest.ECG readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/EcgDataTest/testEcgReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-ecg-readiness-order-exact.xcresult`.
- PPI parser readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, and Android/iOS/common consumer ownership instead of set equality or broad path containment checks in the shared common gate and Android/iOS PPI consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PpiParserCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.PpiDataTest.PPI readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/PpiDataTest/testPpiReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-ppi-readiness-order-exact.xcresult`.
- Pressure/temperature parser readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, and Android pressure/Android temperature/iOS/common consumer ownership instead of set equality or broad path containment checks in the shared common gate and Android/iOS scalar consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.PressureTemperatureParserCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.PressureDataTest.pressure temperature readiness manifest is pinned before scalar parser migration' --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.TemperatureDataTest.pressure temperature readiness manifest is pinned before scalar parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/TemperatureDataTest/testPressureTemperatureReadinessManifestIsPinnedBeforeScalarParserMigration -resultBundlePath /tmp/polar-ios-pressure-temperature-readiness-order-exact.xcresult`.
- Skin-temperature parser readiness coverage now asserts exact policy-vector path order, exact required/covered behavior-family order, and Android/iOS/common consumer ownership instead of set equality or broad path containment checks in the shared common gate and Android/iOS skin-temperature consumers; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.SkinTemperatureParserCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.gatt.client.pmd.model.SkinTemperatureDataTest.skin temperature readiness manifest is pinned before parser migration' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -quiet -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/SkinTemperatureDataTest/testSkinTemperatureReadinessManifestIsPinnedBeforeParserMigration -resultBundlePath /tmp/polar-ios-skin-temperature-readiness-order-exact.xcresult`.
- Shared parser ownership coverage now asserts exact migration ownership text for ACC, ECG, GYR, MAG, offline HR, PPG, PPI, skin-temperature, and pressure/temperature platform-specific vectors plus the shared GNSS location ownership text, and pressure/temperature compressed scalar deferral coverage now asserts exact notes instead of ownership/note substring checks.
- Shared advertisement malformed-GPB coverage and fake-transport readiness coverage now assert exact common malformed-advertisement policy text and exact per-vector common runtime-prototype status/wall-clock-safety metadata instead of substring or raw JSON containment checks.

## Work Left Before Claiming Full Coverage Preparation Complete

- Keep full iOS XCTest in the required validation set for future slices; `swiftc -parse` is only a syntax gate and must not replace the passing simulator XCTest command.
- The full iOS XCTest validation requirement is now guarded by `GoldenVectorMigrationPolicyTest.kt` so `KmpValidationCommands.md` must keep the simulator `xcodebuild test` command and must keep syntax parsing plus the Xcode infrastructure probe subordinate to the XCTest execution gate.
- The hardware/device smoke boundary is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so physical BLE hardware validation remains adapter-wiring smoke evidence only and cannot replace deterministic golden-vector, shared common, Android facade, or iOS XCTest characterization gates.
- The Gradle batching boundary is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so validation remains focused on library/shared-module gates and avoids broad app/example Gradle tasks unless a slice actually changes those surfaces.
- The TDD minimum validation set is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so merge readiness cannot drop KMP common tests, existing Android tests, existing iOS tests or equivalent Apple-platform commands, reviewed golden-vector API contracts, or the no-unrelated-platform-refactor rule.
- Release-readiness checklist items now have production shared-consumption evidence: `scripts/verify_android_example_aar_consumption.sh` proves Android example local-AAR consumption with the SDK/shared AAR pair, `PolarBleSdk.podspec` plus `pod lib lint PolarBleSdk.podspec --allow-warnings` proves the CocoaPods shared-framework path, `swift package describe` keeps SwiftPM manifest validation explicit while `documentation/KmpSharedArtifactConsumption.md` documents SwiftPM/watchOS as fallback-only for shared consumption, generated API docs stay untouched because no public SDK APIs changed, and rollback paths are documented for every shared-module adoption step.
- Android facade command/file-operation selection now executes through existing shared runtime planner tokens for H10 recording queries, disk/time query IDs, local-time write query IDs, reset/sync notifications, REST request paths, direct low-level file GET/PUT/REMOVE builders, and stored-data cleanup `REMOVE` operations for telemetry, SDLOGS, and date-folder deletion; `sources/Android/android-communications/library/src/test/java/com/polar/sdk/impl/utils/PolarRuntimePlannerAdapterTest.kt` pins the Android protobuf enum mapping, while BLE transport calls, protobuf payload builders, filesystem capability gates, directory traversal, and public error mapping remain platform-owned.
- iOS public facade, backup manager, lower production utility planning, and stream runtime policy hooks now route through the `PolarRuntimePlanner` umbrella for user-device-settings, disk/time, command/reset/sync/H10, offline-trigger, stored-data cleanup filters, firmware ordering/workflow progress, REST service requests, low-level file facade operations, file runtime-error hooks, PSFTP write-progress/ack policy hooks, D2H notification mapping, offline-exercise file headers, automatic-sample path planning, activity path planning, sleep/nightly path planning, skin-temperature path planning, training-session, test, watch-face, firmware device-info file planning, stream subscription terminal decisions, stream cancellation, stream disconnect, duplicate completion, and post-completion emission policy; `iOSCommunications` now compiles `PolarRuntimePlanner`, command/disk-time planners, and `pftp_notification.pb.swift`, focused validation passed with `xcodebuild build -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/polar-ios-runtime-umbrella-utils -quiet`, `xcodebuild test -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme PolarBleSdk -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testRuntimePlannerFacadeDelegatesToSpecializedRuntimePlanners -only-testing:PolarBleSdkTests/PolarFirmwareUpdateUtilsTest -derivedDataPath /tmp/polar-ios-runtime-umbrella-utils-tests -resultBundlePath /tmp/polar-ios-runtime-umbrella-utils-tests.xcresult -quiet`, and `xcodebuild test -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme iOSCommunications -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' -only-testing:iOSCommunicationsTests/StreamContinuationListTest -derivedDataPath /tmp/polar-ios-stream-runtime-facade -resultBundlePath /tmp/polar-ios-stream-runtime-facade.xcresult -quiet`, while BLE transport calls, stream lifecycle execution, protobuf builders/parsers, filesystem capability gates, and public error mapping remain platform-owned.
- iOS training-session shared consumption now enters through the `PolarRuntimePlanner` umbrella for payload fetch order, payload parser-case selection, delete-parent/remove path planning, progress clamping, reference date filtering, reference discovery, and training/exercise data-type lookup; `PolarTrainingSessionUtilsTests.testTrainingSessionProgressPercentUsesSharedClampPolicy` pins the umbrella route while PSFTP traversal, protobuf/gzip decoding, progress delivery, public model construction, and unlinked fallback behavior remain Swift-owned.
- Android training-session shared consumption now enters through `PolarRuntimePlannerAdapter` for root-path selection, reference discovery, payload fetch order, payload parser/encoding selection, exercise data-type filenames, delete-parent/remove path planning, progress clamping, and reference date filtering; `PolarTrainingSessionUtilsTest` pins the adapter route while PSFTP traversal, protobuf/gzip decoding, progress delivery, public model construction, and public error behavior remain Android-owned.
- Android firmware utility shared consumption now enters through `PolarRuntimePlannerAdapter` for firmware file priority, available-version comparison, and device protobuf version string projection; `PolarRuntimePlannerAdapterTest` pins the adapter route while PSFTP reads, device-info protobuf parsing, zip extraction, BLE writes, progress delivery, and public update errors remain Android-owned.
- Android impl D2H notification classification now enters through `PolarRuntimePlannerAdapter` for notification type lookup, parsed-protobuf selection, REST event notification filtering, generic D2H emission planning, and exercise-status notification filtering; `PolarRuntimePlannerAdapterTest` pins the adapter route while raw notification observation, public enum compatibility, platform protobuf parsing, stream collection, and public error behavior remain Android-owned.
- Android user-device-settings enum and payload planning now enters through `PolarRuntimePlannerAdapter` for automatic OHR state names, device-location name/value round-trips, USB mode names, automatic-training-detection mode names, and existing payload-field vectors; `BDBleApiImplTest` and `PolarRuntimePlannerAdapterTest` pin the adapter route while protobuf builders/parsers, filesystem capability gates, daylight-saving time source, PSFTP writes, and public error mapping remain Android-owned.
- Android watch-face field normalization now enters through `PolarRuntimePlannerAdapter` for nullable/default field projection before Android `WatchfaceConfigFields` construction; `PolarRuntimePlannerAdapterTest` pins the adapter route while FlatBuffer parsing/building, KVTX script framing, PSFTP reads/writes, public complication enum compatibility, and public errors remain Android-owned.
- Android REST event payload selection now enters through `PolarRuntimePlannerAdapter` for uncompressed payload passthrough after shared D2H notification filtering; `PolarDeviceRestApiUtilsTest` and `PolarRuntimePlannerAdapterTest` pin the adapter route while REST event protobuf parsing, gzip decompression, flow collection, and public event string/error behavior remain Android-owned.
- Android offline-recording metadata grouping now enters through `PolarRuntimePlannerAdapter` for filename measurement classification, V1 grouped file entries, and PMDFILES V2 parsing; `PolarOfflineRecordingUtilsTest` and `PolarRuntimePlannerAdapterTest` pin the adapter route while PSFTP traversal, public `PolarOfflineRecordingEntry` construction, file bytes/text decoding, protobuf offline-recording data parsing, and public invalid-type mapping remain Android-owned.
- Android sleep result policy now enters through `PolarRuntimePlannerAdapter` for sleep offset projection, original-range inclusion, and sleep-skin-temperature inclusion; `PolarSleepUtilsTest` and `PolarRuntimePlannerAdapterTest` pin the adapter route while PSFTP reads, sleep protobuf parsing, public sleep model construction, and fallback-on-read-error behavior remain Android-owned.
- Android SpO2 test projection now enters through `PolarRuntimePlannerAdapter` for proto-field name/value projection into neutral policy strings; `PolarTestUtilsTest` and `PolarRuntimePlannerAdapterTest` pin the adapter route while PSFTP directory traversal, protobuf parsing, epoch fallback time conversion, Android public enum mapping, and public `PolarSpo2TestData` construction remain Android-owned.
- Android KVTX byte-codec usage now enters through `PolarRuntimePlannerAdapter` for write-and-commit script building, value extraction, and little-endian `u32` encoding; `KvtxScriptUtilsTest` and `PolarRuntimePlannerAdapterTest` pin the adapter route while watch-face PSFTP reads/writes, FlatBuffer config parsing/building, public watch-face model mapping, and platform-specific malformed-script error behavior remain Android-owned.
- Android time conversion field policy now enters through `PolarRuntimePlannerAdapter` for date/time field DTO creation, millisecond-to-nanosecond conversion, seconds-to-minutes conversion, and duration-to-millisecond projection; `PolarTimeUtilsTest` and `PolarRuntimePlannerAdapterTest` pin the adapter route while Java `Calendar`/`LocalDateTime`/`Instant`, protobuf builder construction, timezone object creation, and public Java time return types remain Android-owned.
- Android backup restore DTO bridging now enters through `PolarRuntimePlannerAdapter.PlannedBackupRestoreFile`, with conversion to shared `PolarBackupRestoreFile` contained inside the adapter; `PolarBackupManagerTest` and `PolarRuntimePlannerAdapterTest` pin the route while PSFTP traversal, backup file byte loading, stream writes, logging, restore-failure continuation, and public backup file data remain Android-owned.
- Android training-session DTO bridging now enters through `PolarRuntimePlannerAdapter` for file-entry inputs, reference outputs, exercise-reference outputs, and payload-fetch-order reference inputs, with conversion to shared training DTOs contained inside the adapter; `PolarTrainingSessionUtilsTest` and `PolarRuntimePlannerAdapterTest` pin the route while PSFTP traversal, gzip/protobuf decoding, public training-session model construction, progress delivery, and delete execution remain Android-owned.
- Android API model direct shared access now starts routing through `PolarSdkModelAdapter` for disk-space projection, Device ID UUID construction, and D2H notification type lookup, reducing public API model coupling to shared internals while preserving public Android classes and keeping shared conversion contained in the SDK model adapter; `PolarDiskSpaceTest`, `PolarDeviceUuidTest`, `DeviceIdGoldenVectorTest`, and `PolarD2HNotificationsUtilsTest` pin the route while protobuf parsing, enum construction, flow delivery, and public error behavior remain Android-owned.
- Android REST public model projection now enters through `PolarSdkModelAdapter` for service-list names/paths, service-description events/endpoints/actions, action names/paths, event details, and event triggers; `PolarDeviceRestApiUtilsTest` and `PolarSdkModelAdapterTest` pin the route while Gson/dictionary decoding, wrong-type Android empty-default policy, REST event gzip decompression, and public REST model surfaces remain Android-owned.
- Android SD log trigger and magnetometer-frequency enum normalization now enters through `PolarSdkModelAdapter`; `BDBleApiImplTest` and `PolarSdkModelAdapterTest` pin the route while Android still owns protobuf parsing/building and preserves unknown enum fallback behavior.
- Android first-time-use and watch-face public enum lookup now enters through `PolarSdkModelAdapter`; `PolarFirstTimeUseConfigTest`, `PolarWatchFaceUtilsTest`, and `PolarSdkModelAdapterTest` pin the route while Android still owns protobuf builders/parsers, public enum projection, KVTX script handling, and unknown-complication fallback.
- Android activity, sleep, exercise-session, automatic-HR, daily-summary, and PPI public enum lookup now enters through `PolarSdkModelAdapter`; `PolarExerciseSessionTest`, `PolarSleepUtilsTest`, `PolarActivityUtilsTest`, `PolarAutomaticSamplesUtilsTest`, and `PolarSdkModelAdapterTest` pin the route while Android still owns protobuf parsing, public enum construction, sample decompression, and unknown-value policy.
- Android user-device-settings public DTO projection now enters through `PolarSdkModelAdapter`; `PolarUserDeviceSettingsTest` and `PolarSdkModelAdapterTest` pin presence-preserving parse/serialize behavior while Android still owns protobuf builders/parsers, timestamp construction, read/write execution, and public settings defaults.
- Android SpO2 public enum lookup now enters through `PolarSdkModelAdapter`; `PolarTestUtilsTest` and `PolarSdkModelAdapterTest` pin the route while Android still owns protobuf parsing, public model construction, optional-field policy, and unknown enum handling.
- Android training-session public exercise file-name lookup now enters through `PolarSdkModelAdapter`; `PolarTrainingSessionUtilsTest` and `PolarSdkModelAdapterTest` pin the route while Android still owns public DTO construction and protobuf/gzip parsing.
- Android skin-temperature public enum and sample projection now enters through `PolarSdkModelAdapter`; `PolarSkinTemperatureUtilsTest` and `PolarSdkModelAdapterTest` pin the route while Android still owns protobuf parsing, public model construction, and unknown enum null policy.
- Android user-device-settings facade read/write execution now consumes the `PolarRuntimePlannerAdapter.planUserDeviceSettingsRead` and `planUserDeviceSettingsWrite` operations directly instead of treating shared planning as a side-effect hook; `BDBleApiImplTest` and `PolarRuntimePlannerAdapterTest` pin the same public GET/PUT path and write-failure-after-payload behavior while protobuf building/parsing, filesystem capability gates, and public errors remain Android-owned.
- iOS user-device-settings facade and utility read/write execution now consumes the shared `PolarRuntimePlanner.userDeviceSettings` terminal before each whole-settings and setting-specific read/write operation, while continuing to consume shared-planned GET/PUT operations and keeping protobuf mutation/parsing, filesystem capability gates, PSFTP write streams, daylight-saving time-source behavior, and public errors Swift-owned.
- iOS REST and low-level file facade execution now consumes shared `restFacadeGet` and `fileFacade` terminal statuses before REST service GETs, notification PUTs, file reads, file writes, directory lists, and deletes, while JSON/protobuf decoding, write streams, PSFTP transport, directory traversal, response-error preservation, and public Swift error mapping remain platform-owned.
- iOS command facade execution now consumes shared query, reset, sync-start, and sync-stop terminal statuses before using shared query values or notification plans for H10 recording, live exercise, firmware prepare, reset, SD-log sync, and explicit sync notification helpers, while protobuf parameter builders, PSFTP query/notification transport, capability gates, and public error mapping remain Swift-owned.
- iOS offline exercise V2 now consumes shared command and file-facade terminal statuses before start, stop, status, fetch, remove, and device-info support checks, while protobuf request/response mapping, PSFTP query/request transport, recursive file listing, support polling, and public error mapping remain Swift-owned.
- iOS disk/time facade execution now consumes shared set-local-time and disk/time query terminal statuses before using shared query values for H10 local time, PFSv2 system/local time, local-time reads, zoned local-time reads, and disk-space reads, while protobuf timestamp conversion, timezone offset decoding, PSFTP query transport, capability gates, and public errors remain Swift-owned.
- iOS PSFTP write sites now consume shared write-progress completion milestones and ACK terminal status through `PolarRuntimePlanner.ensurePsFtpWriteRuntimePlan`, while actual `BlePsFtpClient.write` stream consumption, emitted progress callbacks, retry/reboot behavior, and public error mapping remain Swift-owned.
- Android stored-data cleanup scenario planning now returns concrete PSFTP operations from `PolarRuntimePlannerAdapter.planStoredDataCleanup` instead of acting as a side-effect-only shared hook; `BDBleApiImplTest` and `PolarRuntimePlannerAdapterTest` pin the same Android traversal, remove, list-failure, and empty-parent behavior while recursive list execution, protobuf automatic-sample reads, and public cleanup error policy remain Android-owned.
- iOS stored-data cleanup now consumes shared terminal statuses for root filtering, activity pruning, automatic-sample pruning, empty-day folder removal, and telemetry cleanup before PSFTP execution, while recursive traversal, protobuf automatic-sample reads, delete operations, empty-parent cleanup, and public error propagation remain Swift-owned.
- Android firmware workflow now consumes the returned `PolarRuntimePlannerAdapter.planFirmwareWorkflow` write paths for actual PUT operation selection, shared `check-update-available`/`check-update-not-available` plans before public availability mapping, the shared `retryable-server-failure` terminal plan before preserving retryable firmware-check failures, the shared `download-failure` terminal plan before preserving package-download failures, the shared `cancel-after-package-fetch-cleans-up-before-ble-write` terminal/cleanup plan before rethrowing package-fetch cancellation, the shared `system-update-reboot-response-is-success` and `battery-too-low-response-is-terminal-failure` terminal plans before platform write-error mapping, and the shared `empty-or-invalid-zip` terminal plan before public invalid-package `FwUpdateNotAvailable` emission, instead of using the shared workflow call only as a side-effect hook; `PolarRuntimePlannerAdapterTest` plus `BDBleApiImplTest` pin statuses, ordered writes, terminal success, terminal-error absence, check status mapping, retryable failure mapping, cancellation cleanup count, write-terminal mapping, and no BLE writes for firmware-check, package-download, cancellation, or invalid-package failures while network/download/zip/BLE execution, progress emission, reboot handling, and public error mapping remain Android-owned.
- iOS firmware package extraction now skips non-payload ZIP entries through shared `PolarWorkflowRuntimePlanning.firmwarePackageEntryIsPayload`, matching Android's extraction filter before iOS applies shared payload ordering and write-path planning; ZIP filesystem access, package download, BLE writes, retry scheduling, progress/status emission, reboot waits, and public error mapping remain platform-owned.
- iOS firmware device-info reads now consume the shared `fileFacade` terminal status before executing the shared-planned GET operation, while PSFTP request execution, device-info protobuf parsing, nullable failure behavior, and public firmware info projection remain Swift-owned.
- iOS firmware workflow now consumes the shared workflow terminal status before public availability mapping through `check-update-available`/`check-update-not-available`, before preserving retryable firmware-check failures through the shared `retryable-server-failure` terminal-error, before applying shared write-path ordering, before invoking platform PSFTP writes, before preserving package-download failures through the shared `download-failure` plan, before rethrowing package-fetch cancellation through the shared `cancel-after-package-fetch-cleans-up-before-ble-write` terminal-error, before write-error mapping through `system-update-reboot-response-is-success` and the shared `battery-too-low-response-is-terminal-failure` terminal-error, and before public invalid-package `fwUpdateNotAvailable` emission through the shared `empty-or-invalid-zip` plan, while network/download/ZIP access, write streams, progress/status emission, cancellation cleanup execution, reboot waits, and public error mapping remain Swift-owned.
- Android offline-trigger get/set planning now consumes returned `PolarRuntimePlannerAdapter.planOfflineTriggerSet` and `planOfflineTriggerGet` command and feature decisions instead of using shared runtime planning only as a side-effect hook; `BDBleApiImplTest` and `PolarRuntimePlannerAdapterTest` pin planned set commands, shared enabled-feature projection, and enabled-GYRO exclusion while PMD client calls, packet building/parsing, selected settings, optional secret mapping, and public error propagation remain Android-owned.
- iOS offline-trigger get/set facades now consume the shared runtime terminal returned by `PolarRuntimePlanner.offlineTriggerSet` and `offlineTriggerGet` before invoking platform PMD control-point execution; PMD client calls, packet building/parsing, selected settings, optional secret mapping, and public error propagation remain Swift-owned.
- iOS generic stream runtime now consumes shared cancellation, disconnect, duplicate-completion, and post-completion-emission decisions in `StreamContinuationList` and `MulticastAsyncStream`, while lock-protected observer storage, upstream task lifetime, transport connection checks, and BLE/session ownership remain Swift-owned.
- Android setting-specific user-device-settings setters now pass the returned `planUserDeviceSettingsReadThenWrite` PUT operation into the production write helper instead of keeping that batch plan as a side-effect-only hook; existing facade tests pin read-failure-without-write and write-failure-after-payload behavior while Android still owns protobuf mutation, filesystem capability gates, PSFTP write streams, progress, ACK handling, and public errors.
- Android and iOS training-session payload fetch-order planning now delegates through shared `PolarTrainingSessionModels.payloadFetchOrder`; Android `PolarTrainingSessionUtilsTest` pins public-reference-to-shared order mapping with `:library:testSdkDebugUnitTest --tests com.polar.sdk.api.model.utils.PolarTrainingSessionUtilsTest`, iOS `PolarTrainingSessionUtilsTests` pins the `PolarIosSharedBridge.trainingSessionPayloadFetchOrder` path with focused XCTest, and protobuf/gzip payload parsing plus malformed component projection remain platform-owned until common protobuf/gzip production dependencies are added.
- Android and iOS training-session progress percentage clamping now delegates through shared `PolarTrainingSessionModels.progressPercent`; platform code still owns PSFTP reads, protobuf/gzip decoding, progress callback delivery, and public model/error construction.
- Android and iOS training-session reference date filtering, including the linked iOS fallback traversal path, now delegates inclusive `yyyy-MM-dd`/`yyyyMMdd` matching through shared `PolarTrainingSessionModels.referenceDateMatches`; the unlinked iOS fallback now reuses the shared-backed basic date-range utility instead of a global `DateComponents` comparison extension. Platform code still owns PSFTP traversal, `LocalDate`/`Date` conversion, public reference construction, protobuf/gzip decoding, progress delivery, and public model/error construction.
- Android and iOS backup root-path/default selection, BACKUP.TXT text parsing, and backup file path splitting now delegate through shared `PolarWorkflowRuntimePlanning.backupRootPaths`, `parseBackupTextForAndroid`, `parseBackupTextForIos`, and `backupFilePath`; shared backup policy coverage also exercises production `expandBackupEntries`, `readBackupFiles`, and `planBackupRestore` for command/payload order and restore-failure aggregation, while Android `PolarBackupManagerTest` and iOS `PolarBackupManagerTest` keep platform restore-failure compatibility pinned. Recursive traversal, directory protobuf parsing, file reads/writes, and public restore error mapping remain adapter-owned.
- Android backup restore now consumes shared `PolarWorkflowRuntimePlanning.planBackupRestore` as a full batch through `PolarRuntimePlannerAdapter.planBackupRestoreWrites`, preserving shared PUT order and payload hex planning while PSFTP write execution, payload streams, logging, and Android restore-failure aggregation remain platform-owned.
- Linked iOS backup restore now consumes shared `PolarWorkflowRuntimePlanning.planBackupRestore` as a full batch through `PolarIosSharedBridge.planRuntimeBackupRestoreOperations` and `PolarRuntimePlanner.backupRestoreWrites`, preserving shared PUT order and payload hex planning while SwiftPM/watchOS fallback, PSFTP write execution, payload streams, logging, and iOS restore-failure aggregation remain platform-owned.
- iOS backup traversal now consumes shared `fileFacade` terminal statuses before root directory, backup file, and recursive directory GET execution, while recursive traversal, BACKUP.TXT streaming, directory protobuf parsing, file reads/writes, partial backup fallback, and restore-failure aggregation remain Swift-owned.
- iOS production type utilities now delegate compatible signed-int and unsigned-long conversions to `PolarIosSharedBridge`/`PolarTypeUtils` when `PolarBleSdkShared.framework` is linked; empty/oversized precondition behavior and the documented iOS unsigned-int high-bit platform difference remain Swift-owned until a future compatibility slice deliberately changes them.
- Migration stop conditions are now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so undocumented Android/iOS behavior disagreement, missing parser golden-vector coverage, platform APIs in shared implementations, unintended facade behavior changes, and hardware-only validation for fakeable logic must halt a slice before shared-code migration continues.
- The per-slice TDD and review checklists are now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so future slices must choose one behavior family, list Android/iOS implementation and test evidence, add platform characterization, shared vectors, and failing common tests before implementation/delegation, rerun Android/iOS/KMP tests, remove duplicate platform logic only after both facades pass, and keep public API compatibility, platform BLE ownership, hidden platform dependency, error mapping, cancellation/timeout, and common-build-metadata review gates visible.
- The suggested migration slice order is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so deterministic device-ID/time/capability/settings/parser/model slices stay ahead of fake transport, runtime state machines, and public API compatibility adapters.
- Product capability lookup coverage now has shared golden vectors and Android/iOS/common consumers for filesystem type mapping, unknown filesystem fallback, missing-device defaults, case-insensitive device type, recording defaults, firmware-update defaults, activity-data defaults, sensor-device defaults, version-mismatch user-config merge behavior, resource-override platform ownership, vector references, and compile verification; focused validation passes with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :shared:jvmTest --tests 'com.polar.sharedtest.DeviceCapabilitiesCommonPolicyTest' :library:testSdkDebugUnitTest --tests 'com.polar.androidcommunications.api.ble.model.polar.BlePolarDeviceCapabilitiesUtilityTest' --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all` and `xcodebuild test -workspace sources/iOS/ios-communications/iOSCommunications.xcworkspace -scheme PolarBleSdk -destination 'platform=iOS Simulator,id=0DB7D4B5-705D-4195-8F51-75425EBF2B96,arch=arm64' -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testCapabilityGoldenVectorsMatchIOSBehavior -only-testing:PolarBleSdkTests/PolarBleApiImplTests/testCapabilityLookupReadinessManifestIsPinnedBeforeCapabilityMigration`; resource selection remains platform-owned, and shared KMP capability code must receive an already selected config unless a later common resource-loading slice deliberately adds Android AssetManager/external-file and iOS Bundle.main/SDK-bundle/sandbox tests.
- Android service-client identifier routing now delegates neutral device-id/platform-specific/invalid classification to shared `PolarDeviceId.classifyIdentifier`, while Android keeps the exact Bluetooth-address gate and public invalid-argument mapping for non-Android platform identifiers such as CoreBluetooth UUID strings.
- iOS service-client identifier routing now delegates the same neutral classification through `PolarIosSharedBridge`, while iOS keeps the exact CoreBluetooth UUID gate and public invalid-argument mapping for Android Bluetooth-address strings.
- The first recommended TDD slice guidance is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so migration starts with low-risk deterministic parser/model coverage such as `PolarDeviceUuid`, time utilities, or a PMD sensor parser, and explicitly does not start with BLE scanning, connection lifecycle, firmware network calls, or public API redesign.
- The TDD coverage expectations are now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so input coverage remains more important than line coverage, parser slices keep supported frame versions, field boundaries, malformed payloads, empty payloads, and unknown enum values covered, and runtime slices keep state transitions plus cancellation paths covered.
- The TDD regression policy is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so behavior changes require current-behavior characterization, updated expected output, written migration-slice rationale, and release notes or migration-guide updates for consumer-visible changes.
- Keep `BLE device session lifecycle`, `GATT clients`, Android Bluedroid host behavior, and iOS CoreBluetooth host behavior platform-owned unless a later slice extracts a pure codec or deterministic state machine with common fake-transport tests, Android compatibility tests, iOS compatibility tests, and an updated `KmpCoverageInventory.md` row.
- Android Bluedroid dead/unknown GATT cleanup is now pinned as platform-owned host behavior: `GattCallbackTest.kt` covers unknown service-discovery and PHY callbacks closing their host `BluetoothGatt`, and `GattCallback.kt` performs that cleanup without shared KMP delegation.
- Android `ChannelUtils.emitNext` now delegates empty-observer late-emission classification to shared `PolarStreamRuntimePlanning.planPostCompletionEmissionSuppression`, matching the linked iOS stream helper while keeping channel delivery, observer storage, and cancellation platform-owned.
- Android `ChannelUtils.monitorNotifications` now uses shared `PolarStreamRuntimePlanning.planCheckedSubscription` active-observer output to gate observer registration after the platform connection probe, while transport `isConnected`, channel delivery, observer storage, cancellation, and public `BleDisconnected` mapping remain platform-owned.
- Android PSFTP write progress emission now delegates first/final/throttled progress decisions to shared `PolarWorkflowRuntimePlanning.shouldEmitPsFtpWriteProgress`, preserving the existing Android negative-header/final-payload public progress split while keeping MTU writes, ACK waits, cancellation, and BLE transport platform-owned.
- Linked iOS PSFTP write progress emission now clamps Swift RFC60/RFC76 stream positions to the shared payload-count progress policy before yielding public progress values, preserving the vector-pinned `0, payloadSize - 1, payloadSize` sequence while MTU writes, ACK waits, cancellation, and BLE transport remain Swift-owned.
- Android and iOS PSFTP write-ack success terminal classification now delegates through shared `PolarWorkflowRuntimePlanning.psFtpWriteAckTerminal` after platform `readResponse` succeeds, while ACK waiting, timeout/error exceptions, MTU writes, cancellation, and BLE transport remain platform-owned.
- Android facade and utility write sites now consume shared PSFTP write progress and ACK terminal decisions through `PolarRuntimePlannerAdapter.ensurePsFtpWriteRuntimePlan` before low-level file, watch-face KVTX, first-time-use, SD-log config, firmware payload, user-device-settings, and backup-restore writes enter platform `BlePsFtpClient.write`, while payload builders, BLE transport, timeout handling, cancellation, and public error mapping remain Android-owned.
- Linked iOS PSFTP wait-notification execution now preserves raw D2H RFC76 packets and delegates complete notification reassembly through shared `PolarWorkflowRuntimePlanning.reassembleNotifications` via `PolarIosSharedBridge.psFtpReassembledNotifications`, while OperationQueue polling, queue lifetime, notification-enable checks, continuation timeout, transport status errors, CoreBluetooth delivery, and SwiftPM/watchOS fallback assembly remain Swift-owned.
- Android and iOS PSFTP write timeout selection is now directly pinned at the production `BlePsFtpClient.getWriteTimeoutForFilePath` and `BlePsFtpUtility.writeTimeoutSeconds` boundaries, proving the shared `PolarWorkflowRuntimePlanning.psFtpWriteTimeoutSeconds` extended `/SYNCPART.TGZ` policy is consumed before platform-owned MTU writes, ACK waits, scanner pause/resume cleanup, timeout exceptions, and BLE transport execution.
- Android and linked iOS PSFTP continuation-timeout and write-ack-timeout vectors now execute through short injected platform protocol timeouts, preserving Android `Notification receive failed` continuation-timeout and `PftpOperationTimeout` write-ack behavior plus iOS `responseError(-1)` continuation-timeout and `AtomicIntegerException.waitTimeout` write-ack behavior before shared PSFTP runtime orchestration moves.
- The backlog's platform-owned host boundary is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so Android Bluedroid host behavior, iOS CoreBluetooth host behavior, GATT client host interactions, platform identifier routing, BLE device lifecycle, and GATT clients remain platform-specific unless a future slice defines a pure codec or deterministic state-machine contract with common fake-transport coverage.
- Shared `commonMain` production portability is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so shared production code cannot introduce Android/JVM/Apple-only APIs, CoreBluetooth/UIKit/CryptoKit references, Bluetooth host APIs, or global-main concurrency shortcuts before the migration plan's common-code boundary is deliberately updated.
- For public facade delegation, add operation-specific Android facade tests, iOS facade tests, and shared fake-transport tests for success, request failure, response failure, write failure, cleanup, cancellation, timeout, and platform-specific public error mapping before moving any corresponding runtime code to KMP.
- The fake-transport plan's pre-migration runtime/facade rules are now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so runtime slices must name implemented matrix rows, facade slices must keep platform and common fake-transport tests for success/error/cancellation/timeout where applicable, platform-specific areas must update `KmpCoverageInventory.md`, and stream/task runtime tests must assert cleanup or cancellation propagation.
- Shared common vector consumers are now structurally guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so any vector with `input.cases`, `input.scenarios`, `input.requests`, `input.operations`, or common-prototype `cases` and a `com.polar.sharedtest.*` consumer must have every exact behavior ID referenced by the owning shared common test instead of relying on implicit iteration; validation passes with `ruby scripts/kmp_non_gradle_checks.rb`, `git diff --check`, and from `sources/Android/android-communications` with `ANDROID_HOME="$HOME/Library/Android/sdk" ANDROID_SDK_ROOT="$HOME/Library/Android/sdk" ./gradlew :library:testSdkDebugUnitTest --tests 'com.polar.sdk.api.model.utils.GoldenVectorMigrationPolicyTest' --warning-mode all`.
- For REST/file runtime delegation, keep public error shape, empty-response behavior, malformed directory/protobuf parsing, response-error mapping, REST event streaming, unsupported-service mapping, and write-stream failure behavior pinned through both platform facades and shared common tests; `RestServiceMappingCommonPolicyTest.kt`, `RestRequestTransportPolicyCommonTest.kt`, `RestFacadeRuntimePolicyCommonTest.kt`, and `FileRuntimeErrorPolicyCommonTest.kt` are now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so REST request production shared command/error planning, sleep REST root/recording-state subscription/stop path planning, REST facade success/failure/response-error/empty-success/malformed-success scenarios, empty-success policy, response-error payload status/message, file traversal, read/write/delete production shared command planning, directory-missing and malformed-directory policy, write-stream failure, facade-error deferral, and compile-verification terms cannot drift out of the shared common policy.
- For PSFTP runtime delegation, add or keep fake-clock/injectable-timeout facade compatibility for initial silence, continuation timeout, write-ack timeout, notification error splits, write progress behavior, operation cleanup, cancellation cleanup, and no leaked pending operation after timeout; `PsFtpByteCodecCommonPolicyTest.kt` and `PsFtpRuntimePolicyCommonTest.kt` are now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so RFC76 header/status/error-frame parsing, RFC60 complete-message stream encoding, Android file-data append policy, MTU frame splitting, sequence wrap, request/response reassembly through production shared `PolarWorkflowRuntimePlanning.reassembleRequestResponse`, response-error mapping, notification ordering/reassembly/error split through production shared `reassembleNotifications`, consumer-owned timeout cleanup through production shared `planConsumerTimeoutObserverCleanup`, continuation timeout, write progress split through production shared `planPsFtpWriteProgress`, interruption/transport/write-ack failures through production shared write planners, platform client references, and compile-verification terms cannot drift out of the shared common PSFTP policy.
- The PSFTP timeout runtime ledger is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so initial-silence, continuation-timeout, and write-ack-timeout rows must keep fake-clock or injectable-timeout facade compatibility gates explicit before production PSFTP runtime delegation.
- For firmware workflow delegation, keep production-injectable network, zip/filesystem, BLE writer, and progress seams executable while adding terminal failure, reboot, battery-too-low, cleanup, cancellation, and public facade error-mapping tests before moving workflow orchestration to shared code; `FirmwareWorkflowRuntimePolicyCommonTest.kt` now runs `workflow-runtime-policy.json` through production shared `PolarWorkflowRuntimePlanning.planFirmwareWorkflow` and is guarded by `GoldenVectorMigrationPolicyTest.kt` plus `scripts/kmp_non_gradle_checks.rb` so fake network availability, fake filesystem/zip extraction, BLE write progress, shared retry delay planning and platform execution, cancellation cleanup before BLE writes, retryable server failure, reboot, battery-too-low, facade-error mapping, and compile-verification terms cannot drift out of the shared common policy. Android and iOS firmware write failure handling now map the shared `battery-too-low`, `success-rebooting`, and non-system reboot terminals through platform-owned `PolarFirmwareUpdateUtils.firmwareWriteFailure` helpers after consuming the shared `system-update-reboot-response-is-success` and `battery-too-low-response-is-terminal-failure` workflow scenarios, while raw PSFTP/GATT writer execution stays platform-owned.
- iOS `FirmwareUpdateApi` now has an injectable `FirmwareUpdateNetworkTransport` seam with tests for check-request construction, 4xx completion, and package-download error propagation, while the production default remains `URLSession.shared`; `PolarBleApiImpl.firmwareUpdateApiFactory` now routes facade package download and firmware-check error handling through an injectable `FirmwareUpdateServicing` seam before package extraction and pins package-download service error propagation, public `checkFwUpdateAvailable`/`checkFwUpdateNotAvailable` planning, public `checkFwUpdateFailed` mapping for retryable firmware service failures through the shared `retryable-server-failure` plan, public `checkFwUpdateFailed` and `updateFirmware` mapping for client/request firmware service failures through the shared `client-request-failure` terminal-error, no package download after check failure, `updateFirmware` terminal `fwUpdateFailed` preservation, `updateFirmware` retryable availability failure handling through the shared `retryable-server-failure` terminal-error plus retry-delay plan, public package-download failure mapping through the shared `download-failure` plan before BLE writes, direct `updateFirmware(_:fromFirmwareURL:)` package-download/invalid/no-payload/cancellation handling before BLE writes without firmware-check lookup, invalid/empty/no-payload package handling through the shared `empty-or-invalid-zip` plan before BLE writes, cancellation during package download through the shared `cancel-after-package-fetch-cleans-up-before-ble-write` plan before BLE writes, public battery-too-low write-terminal mapping, public system-update reboot write-terminal success, public non-system reboot write-terminal failure mapping, and public backup restore write-failure-to-`fwUpdateFailed` mapping through the production writer/backup manager while PSFTP remains platform-owned. Android `BDBleApiImpl.firmwareUpdateApiFactory` now routes firmware check and package download through an injectable `FirmwareUpdateApi` seam, with facade coverage pinning device-info-to-request construction, successful check status mapping through shared check-available/not-available plans, retryable server-failure-to-`CheckFwUpdateFailed`/`FwUpdateFailed` mapping through the shared `retryable-server-failure` plan, client-request-failure-to-`CheckFwUpdateFailed` mapping through the shared `client-request-failure` terminal-error plan, no package download after check failure, `updateFirmware` terminal `FwUpdateFailed` preservation, `updateFirmware` retry execution through shared retry delays, public package-download failure mapping through the shared `download-failure` plan before BLE writes without direct Retrofit construction, direct firmware URL package-download/invalid/no-payload/cancellation handling before BLE writes without firmware-check lookup, invalid/empty/no-payload package handling through the shared `empty-or-invalid-zip` plan before BLE writes, cancellation during package download through the shared `cancel-after-package-fetch-cleans-up-before-ble-write` plan before BLE writes, public battery-too-low write-terminal mapping, public system-update reboot write-terminal success, public non-system reboot write-terminal failure mapping, and public backup restore write-failure-swallowed success after the BLE writer remains platform-owned. `PolarFirmwareUpdateUtils` also exposes an injectable package extractor seam around Zip/FileManager extraction with tests pinning the injected path. `PolarBleApiImpl.firmwareFileWriteStreamFactory` and its injectable progress clock now pin fake BLE writer execution, firmware write paths, shared progress policy, shared retry delay planning, cancellation cleanup, and shared-terminal-to-public-error mapping at the facade write loop while raw PSFTP/GATT writing stays platform-owned. Android and iOS also pin shared-terminal-to-public-error mapping for firmware `BATTERY_TOO_LOW`, the shared `REBOOTING` system-update success terminal, non-system reboot failure, and backup restore failure platform split at the public facade; remaining cross-platform end-to-end public workflow error mapping is still required before firmware workflow delegation can move further.
- The firmware workflow facade ledger is now guarded by `GoldenVectorMigrationPolicyTest.kt` so it must remain explicitly `facade gate open` and name injectable production dependencies plus `FirmwareUpdateApi(transport:)`, `PolarFirmwareUpdateUtils.packageExtractor`, `firmwareFileWriteStreamFactory`, facade progress, cleanup, cancellation, shared-planned retry delay execution, and error-mapping tests before delegation.
- The public facade ledger's current `facade gate open` rows are now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so the firmware workflow row must keep concrete Android, iOS, shared/runtime evidence plus operation-specific migration gates before delegation.
- The public facade ledger's runtime-pinned rows are now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so user-device-settings, stored-data cleanup/deletion, reset/sync/H10 commands, REST, file, disk/time, and offline-trigger rows must keep platform public-error mapping, protobuf/parser ownership, empty-response policy, response-error handling, cleanup/queue split, sync failure split translation, timeout/capability, and conditional cancellation gates explicit before delegation.
- For offline-trigger runtime delegation, Android public facade mapping and get/set error propagation are now pinned through `BDBleApiImplTest.kt`, iOS public facade mapping, secret propagation, set-mode error propagation, and get-status error propagation are pinned through `PolarBleApiImplTests.swift`, byte-level Android/iOS PMD adapter packet framing is pinned through `BlePmdClientTest.kt` and `BlePmdClientTest.swift`, and response-queue cleanup is pinned through Android stale wrong-command discard plus iOS pre-command queue clearing tests; `OfflineTriggerRuntimePolicyCommonTest.kt` now runs `trigger-runtime-policy.json` through production shared `PolarWorkflowRuntimePlanning.planOfflineTriggerRuntime` and is guarded by `GoldenVectorMigrationPolicyTest.kt` plus `scripts/kmp_non_gradle_checks.rb` so typed set-mode, settings writes, optional secret attachment, enabled-feature mapping, facade-error deferral, explicit length-byte split, and compile-verification terms cannot drift; add cancellation coverage only if the production shared trigger runtime introduces cancellable tasks, observers, or streams.
- For command runtime delegation, preserve or explicitly reconcile Android/iOS split behavior for sync initialization, stop, terminate, and failure returns before moving reset, sync, or H10 command planning into production shared runtime; `CommandRuntimePolicyCommonTest.kt` is now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so H10 query success/failure, reset notification flags/failures, sync start/stop platform splits, facade-error mapping, and compile-verification terms cannot drift out of the shared common policy.
- For stored-data cleanup delegation, preserve or explicitly reconcile Android/iOS list-failure behavior and empty-parent remove path spelling before moving traversal/filtering workflows into shared runtime; `StoredDataCleanupRuntimePolicyCommonTest.kt` now runs `cleanup-workflow-policy.json` directly through production shared `PolarWorkflowRuntimePlanning.planStoredDataCleanup` and is guarded by `GoldenVectorMigrationPolicyTest.kt` plus `scripts/kmp_non_gradle_checks.rb` so telemetry, SDLOGS, activity, and automatic-sample filtering plus list-failure/path platform splits, facade-error mapping, and compile-verification terms cannot drift out of the shared common policy.
- For disk/time and user-device-settings runtime delegation, keep filesystem capability gates, protobuf presence/default behavior, daylight-saving time-source behavior, write-failure-after-payload behavior, and public error mapping explicit in platform facade tests; Android `setUserDeviceSettings` and iOS `setPolarUserDeviceSettings` now also pin the legacy whole-settings direct-write facade path and write-failure-after-payload behavior through shared path/write planning. `DiskTimeRuntimePolicyCommonTest.kt` and `UserDeviceSettingsRuntimePolicyCommonTest.kt` are now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so disk-space, local-time, local-time-with-zone, V2/H10 set-local-time sequencing, settings read/write paths, direct whole-settings writes, whole-settings write-failure-after-payload behavior, no-write read failures, daylight-saving payload shape, protobuf-field preservation, facade-error mapping, and compile-verification terms cannot drift out of the shared common policy.
- For model/parser migration, keep every common policy test listed in `KmpFullCoverageTddBacklog.md` compile-verified against its readiness manifest and platform vector references before moving production code; GNSS raw type 0-3 byte decoding is now shared common with Android production delegation, while iOS remains no direct parser surface.
- Add real common protobuf/crypto/codec dependencies where the current vectors intentionally pin behavior but production common code still lacks byte-level parser or cryptographic implementation ownership, especially training-session payload parsing, PMD AES secret handling, and shared FlatBuffer/KVTX byte-identical output decision evidence for the still-deferred watch-face FlatBuffer path. REST gzip/deflate behavior now uses shared KMP platform actual codecs, and KVTX script framing/extraction is owned by shared production code and remains covered by Android, iOS, and common vector consumers.
- Byte-level common dependency deferrals are now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so shared production build dependencies for protobuf, crypto, FlatBuffer, Okio, or kotlinx-io cannot appear before the training-session, PMD secret, and watch-face deferral artifacts are updated with production common codec ownership evidence.
- The full-coverage exit criteria in `KmpCoverageInventory.md` are now guarded by `GoldenVectorMigrationPolicyTest.kt` and `scripts/kmp_non_gradle_checks.rb` so completion cannot be narrowed around current green checks without preserving the Partial-row, parser-vector, cross-platform characterization, public-facade, hardware-smoke, and runnable-validation requirements.
- Run `GoldenVectorMigrationPolicyTest` after any future vector, documentation, shared common test, or runtime-planning metadata change so schema, consumer resolution, commonTest references, platform ownership, and stale-runtime wording stay enforced in Gradle as well as through the Ruby mirror.

## Suggested Next Iteration

Continue facade-specific runtime compatibility from `KmpFakeTransportTestPlan.md`. The next highest-value runtime slice is the next operation family with unresolved facade compatibility or cancellation/timeout behavior; for offline triggers, cleanup is now pinned and cancellation should be added only if shared production delegation introduces cancellable work.
